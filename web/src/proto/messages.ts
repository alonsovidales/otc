// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.7
//   protoc               v6.32.0
// source: messages.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "./google/protobuf/timestamp";

export const protobufPackage = "msg";

export const StatusErrorCode = { DiskError: 0, Syncing: 1, MissingDisk: 2, ErrorInDisk: 3, UNRECOGNIZED: -1 } as const;

export type StatusErrorCode = typeof StatusErrorCode[keyof typeof StatusErrorCode];

export namespace StatusErrorCode {
  export type DiskError = typeof StatusErrorCode.DiskError;
  export type Syncing = typeof StatusErrorCode.Syncing;
  export type MissingDisk = typeof StatusErrorCode.MissingDisk;
  export type ErrorInDisk = typeof StatusErrorCode.ErrorInDisk;
  export type UNRECOGNIZED = typeof StatusErrorCode.UNRECOGNIZED;
}

export function statusErrorCodeFromJSON(object: any): StatusErrorCode {
  switch (object) {
    case 0:
    case "DiskError":
      return StatusErrorCode.DiskError;
    case 1:
    case "Syncing":
      return StatusErrorCode.Syncing;
    case 2:
    case "MissingDisk":
      return StatusErrorCode.MissingDisk;
    case 3:
    case "ErrorInDisk":
      return StatusErrorCode.ErrorInDisk;
    case -1:
    case "UNRECOGNIZED":
    default:
      return StatusErrorCode.UNRECOGNIZED;
  }
}

export function statusErrorCodeToJSON(object: StatusErrorCode): string {
  switch (object) {
    case StatusErrorCode.DiskError:
      return "DiskError";
    case StatusErrorCode.Syncing:
      return "Syncing";
    case StatusErrorCode.MissingDisk:
      return "MissingDisk";
    case StatusErrorCode.ErrorInDisk:
      return "ErrorInDisk";
    case StatusErrorCode.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export const FriendShipStatus = { Pending: 0, Accepted: 1, Blocked: 2, UNRECOGNIZED: -1 } as const;

export type FriendShipStatus = typeof FriendShipStatus[keyof typeof FriendShipStatus];

export namespace FriendShipStatus {
  export type Pending = typeof FriendShipStatus.Pending;
  export type Accepted = typeof FriendShipStatus.Accepted;
  export type Blocked = typeof FriendShipStatus.Blocked;
  export type UNRECOGNIZED = typeof FriendShipStatus.UNRECOGNIZED;
}

export function friendShipStatusFromJSON(object: any): FriendShipStatus {
  switch (object) {
    case 0:
    case "Pending":
      return FriendShipStatus.Pending;
    case 1:
    case "Accepted":
      return FriendShipStatus.Accepted;
    case 2:
    case "Blocked":
      return FriendShipStatus.Blocked;
    case -1:
    case "UNRECOGNIZED":
    default:
      return FriendShipStatus.UNRECOGNIZED;
  }
}

export function friendShipStatusToJSON(object: FriendShipStatus): string {
  switch (object) {
    case FriendShipStatus.Pending:
      return "Pending";
    case FriendShipStatus.Accepted:
      return "Accepted";
    case FriendShipStatus.Blocked:
      return "Blocked";
    case FriendShipStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export const ActionType = { FriendshipReq: 0, LikePub: 1, LikeComm: 2, NewSocialComm: 3, UNRECOGNIZED: -1 } as const;

export type ActionType = typeof ActionType[keyof typeof ActionType];

export namespace ActionType {
  export type FriendshipReq = typeof ActionType.FriendshipReq;
  export type LikePub = typeof ActionType.LikePub;
  export type LikeComm = typeof ActionType.LikeComm;
  export type NewSocialComm = typeof ActionType.NewSocialComm;
  export type UNRECOGNIZED = typeof ActionType.UNRECOGNIZED;
}

export function actionTypeFromJSON(object: any): ActionType {
  switch (object) {
    case 0:
    case "FriendshipReq":
      return ActionType.FriendshipReq;
    case 1:
    case "LikePub":
      return ActionType.LikePub;
    case 2:
    case "LikeComm":
      return ActionType.LikeComm;
    case 3:
    case "NewSocialComm":
      return ActionType.NewSocialComm;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ActionType.UNRECOGNIZED;
  }
}

export function actionTypeToJSON(object: ActionType): string {
  switch (object) {
    case ActionType.FriendshipReq:
      return "FriendshipReq";
    case ActionType.LikePub:
      return "LikePub";
    case ActionType.LikeComm:
      return "LikeComm";
    case ActionType.NewSocialComm:
      return "NewSocialComm";
    case ActionType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export const BridgeOnboardErrorType = {
  generic_error: 0,
  taken_domain: 1,
  secret_missmatch: 2,
  unknown_owner: 3,
  pending_approval: 4,
  UNRECOGNIZED: -1,
} as const;

export type BridgeOnboardErrorType = typeof BridgeOnboardErrorType[keyof typeof BridgeOnboardErrorType];

export namespace BridgeOnboardErrorType {
  export type generic_error = typeof BridgeOnboardErrorType.generic_error;
  export type taken_domain = typeof BridgeOnboardErrorType.taken_domain;
  export type secret_missmatch = typeof BridgeOnboardErrorType.secret_missmatch;
  export type unknown_owner = typeof BridgeOnboardErrorType.unknown_owner;
  export type pending_approval = typeof BridgeOnboardErrorType.pending_approval;
  export type UNRECOGNIZED = typeof BridgeOnboardErrorType.UNRECOGNIZED;
}

export function bridgeOnboardErrorTypeFromJSON(object: any): BridgeOnboardErrorType {
  switch (object) {
    case 0:
    case "generic_error":
      return BridgeOnboardErrorType.generic_error;
    case 1:
    case "taken_domain":
      return BridgeOnboardErrorType.taken_domain;
    case 2:
    case "secret_missmatch":
      return BridgeOnboardErrorType.secret_missmatch;
    case 3:
    case "unknown_owner":
      return BridgeOnboardErrorType.unknown_owner;
    case 4:
    case "pending_approval":
      return BridgeOnboardErrorType.pending_approval;
    case -1:
    case "UNRECOGNIZED":
    default:
      return BridgeOnboardErrorType.UNRECOGNIZED;
  }
}

export function bridgeOnboardErrorTypeToJSON(object: BridgeOnboardErrorType): string {
  switch (object) {
    case BridgeOnboardErrorType.generic_error:
      return "generic_error";
    case BridgeOnboardErrorType.taken_domain:
      return "taken_domain";
    case BridgeOnboardErrorType.secret_missmatch:
      return "secret_missmatch";
    case BridgeOnboardErrorType.unknown_owner:
      return "unknown_owner";
    case BridgeOnboardErrorType.pending_approval:
      return "pending_approval";
    case BridgeOnboardErrorType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface StatusErrors {
  StatusErrorCode: StatusErrorCode;
  Message: string;
}

export interface GetStatus {
}

export interface Status {
  online: boolean;
  localIp: string;
  disks: number;
  errors: StatusErrors[];
  raidSize: number;
  raidUsage: number;
  diskSize: number;
  diskUsage: number;
  cpuUsagePrc: number;
  memSize: number;
  memUsage: number;
}

export interface Auth {
  uuid: string;
  /**
   * RSA-OAEP(SHA-256) ciphertext of the password, encrypted by the client
   * using the public key returned by GetPubKey/PubKey for this connection.
   */
  key: Uint8Array;
  create: boolean;
}

/**
 * GetPubKey requests the ephemeral RSA public key generated for this
 * WebSocket connection. Clients must call this before sending Auth or
 * ChangeKey, and use it to encrypt the key material client-side so that
 * the plaintext password never crosses the bridge, which only relays
 * already-encrypted application payloads between device and client.
 */
export interface GetPubKey {
}

export interface PubKey {
  /** PKIX/SPKI DER-encoded RSA public key, valid only for this connection. */
  publicKey: Uint8Array;
  /**
   * True when this device has no owner secret set up yet (issue #39) —
   * every client already calls GetPubKey before Auth, so this is the
   * cheapest place to tell a fresh device apart from a normal login
   * without a dedicated round trip. Clients should show a first-run setup
   * flow (device password + owner name) instead of a plain sign-in form.
   */
  isNewDevice: boolean;
}

export interface UploadFile {
  path: string;
  content: Uint8Array;
  forceOverride: boolean;
  created?: Date | undefined;
}

export interface DelFile {
  path: string;
}

export interface GetFile {
  path: string;
}

export interface ListFiles {
  path: string;
  recursive: boolean;
}

export interface SearchPhotos {
  tags: string[];
  token: string;
}

export interface ListOfFiles {
  files: File[];
  token: string;
}

export interface File {
  hash: string;
  mime: string;
  created?: Date | undefined;
  modified?: Date | undefined;
  path: string;
  size: number;
  content?: Uint8Array | undefined;
}

export interface Ack {
  ok: boolean;
  errorMsg: string;
}

export interface GetTags {
}

export interface TagsList {
  tags: string[];
}

export interface ChangeKey {
  /** RSA-OAEP(SHA-256) ciphertext, see Auth.key. */
  oldKey: Uint8Array;
  newKey: Uint8Array;
}

export interface NewSocialPublication {
  text: string;
  paths: string[];
}

export interface GetEvents {
  since?: Date | undefined;
  total: number;
}

export interface GetSocialPublicationFiles {
  uuid: string;
}

export interface GetSocialPublications {
  since?: Date | undefined;
  total: number;
  excludeUuids: string[];
}

export interface NewSocialComment {
  pubUuid: string;
  comment: string;
  publisher: string;
}

export interface DelSocialComment {
  commentUuid: string;
}

/** issue #34: delete one of your own posts. */
export interface DelSocialPublication {
  pubUuid: string;
}

export interface DidSendFriendshipReq {
  secret: string;
  domain: string;
}

export interface Friendship {
  originProfile?: Profile | undefined;
  status: FriendShipStatus;
  sent: boolean;
  secret: string;
  latestSync?: Date | undefined;
}

export interface Friendships {
  friendships: Friendship[];
}

export interface FriendshipsList {
}

export interface FriendshipRequest {
  domain: string;
}

export interface FriendshipInterRequest {
  domain: string;
  secret: string;
  originProfile?: Profile | undefined;
}

export interface ChangeFriendStatus {
  domain: string;
  status: FriendShipStatus;
}

export interface LikePublication {
  pubUuid: string;
}

export interface LikeComment {
  commentUuid: string;
}

/** issue #29: "who liked this" for a publication or a comment. */
export interface GetPublicationLikers {
  pubUuid: string;
}

export interface GetCommentLikers {
  commentUuid: string;
}

export interface Likers {
  likers: Profile[];
}

export interface GetSettings {
}

export interface SetSettings {
  domain: string;
}

export interface Settings {
  domain: string;
  /**
   * bridge_secret is the shared secret this device registers with the
   * bridge relay (see BridgeRegister below) — exposed so the setup section
   * can show/update it (issue #40) instead of it only ever being set
   * implicitly at first boot.
   */
  bridgeSecret: string;
}

/**
 * SetBridgeSecret is deliberately its own request rather than a field on
 * SetSettings: domain and bridge_secret used to be updated together (the
 * secret was silently regenerated on every domain change), which broke a
 * device's bridge pairing any time someone just wanted to rename their
 * domain. They're independent settings now.
 */
export interface SetBridgeSecret {
  secret: string;
}

/**
 * Issue #39/#38: first-run storage setup. Listing is safe/read-only (just
 * /sys/block enumeration), but actually building the array is destructive
 * (formats whichever disks are picked), so it's kept as an explicit,
 * separate confirmation step rather than something that happens
 * automatically just because a device was detected.
 */
export interface ListStorageDevices {
}

export interface StorageDevice {
  /** e.g. "/dev/sda" — this exact string is what SetupStorage expects back. */
  path: string;
  sizeBytes: bigint;
  model: string;
}

export interface StorageDevices {
  /**
   * Only devices safe to repurpose: the boot disk itself (and its
   * partitions), loop/ram devices, and anything with no usable size are
   * already filtered out server-side.
   */
  devices: StorageDevice[];
}

/**
 * SetupStorage is destructive on anything selected: 2 devices creates a
 * RAID1 mirror across them, 1 formats and uses that single device alone
 * (no redundancy), 0 leaves storage on the boot disk as-is.
 */
export interface SetupStorage {
  devicePaths: string[];
}

/**
 * Issue #38: first-boot WiFi join, for a device reached over its own
 * temporary "Off The Cloud" access point rather than an already-working
 * network. Scanning is read-only (nmcli dev wifi list needs no special
 * privilege), so it runs directly in the otc service like
 * ListStorageDevices; actually joining a network is privileged the same
 * way storage setup is — see SetWifi.
 */
export interface ListWifiNetworks {
}

export interface WifiNetwork {
  ssid: string;
  /** 0-100, from nmcli's SIGNAL column. */
  signal: number;
  secured: boolean;
}

export interface WifiNetworks {
  networks: WifiNetwork[];
}

/**
 * SetWifi hands the join off to a privileged process the same way
 * SetupStorage does (see storage.RequestSetup) — the otc service can't run
 * `nmcli connection up` itself. An empty password means an open network.
 */
export interface SetWifi {
  ssid: string;
  password: string;
}

export interface BridgeRegister {
  ownerUuid: string;
  domain: string;
  secret: string;
}

export interface BridgeAckOnboard {
  ok: boolean;
}

/**
 * RotateBridgeSecret lets a device that already knows its current secret
 * get a fresh one issued, without an admin manually deleting and re-adding
 * its domain on the bridge's admin panel. Sent bridge-side (not on the
 * device's own client-facing /ws) — `secret` is the CURRENT secret, and is
 * the only thing that authenticates this request: the bridge only replaces
 * it if this matches what it already has on record for owner_uuid+domain,
 * same compare-and-swap spirit as BridgeRegister's existing secret check.
 */
export interface RotateBridgeSecret {
  ownerUuid: string;
  domain: string;
  secret: string;
}

export interface RotateBridgeSecretAck {
  newSecret: string;
}

/**
 * RegenerateBridgeSecret is the client-facing (device /ws, authenticated)
 * request behind the Settings page's "Regenerate" button: the device looks
 * up its own owner_uuid/domain/current secret and does the
 * RotateBridgeSecret round trip to the bridge itself, then persists and
 * returns the new secret — the web app never talks to the bridge directly.
 */
export interface RegenerateBridgeSecret {
}

export interface GetProfile {
}

export interface Profile {
  name: string;
  image?: Uint8Array | undefined;
  text: string;
  domain: string;
}

export interface ShareFilesLink {
  paths: string[];
}

export interface ShareLink {
  link: string;
}

export interface DownloadSharedLink {
  uuid: string;
  secret: string;
}

export interface NewSocial {
  uuid: string;
}

export interface SharedFiles {
  content: Uint8Array;
}

export interface Comment {
  pubUuid: string;
  commentUuid: string;
  comment: string;
  publisher: string;
  likes: number;
  dateTime?:
    | Date
    | undefined;
  /**
   * liked reports whether the requesting viewer already liked this
   * comment, so clients can render a filled vs. outline heart.
   */
  liked: boolean;
}

export interface SocialPublication {
  uuid: string;
  text: string;
  files: File[];
  comments: Comment[];
  likes: number;
  publisher?:
    | Profile
    | undefined;
  /**
   * liked reports whether the requesting viewer already liked this
   * publication, so clients can render a filled vs. outline heart.
   */
  liked: boolean;
  /**
   * own reports whether this is one of the device owner's own posts, so
   * clients know when to offer delete-post / delete-any-comment actions
   * (issues #34/#35).
   */
  own: boolean;
  /**
   * date_time is when the post was published, so clients can show it in
   * the feed (same field shape as Comment.date_time above).
   */
  dateTime?: Date | undefined;
}

export interface SocialPublicationFiles {
  files: File[];
}

/**
 * FileExifInfo is a photo/video's camera/EXIF metadata, computed on demand
 * from the file's own bytes (never stored) for the photo gallery's "More
 * info" panel (issue #41). city/country are filled in from latitude/
 * longitude via an offline reverse-geocode (see geotag/) whenever has_gps
 * is true, the same lookup issue #42 uses to add them as searchable tags.
 */
export interface FileExifInfo {
  cameraMake: string;
  cameraModel: string;
  takenAt?: Date | undefined;
  exposureTime: string;
  fNumber: string;
  iso: number;
  focalLength: string;
  width: number;
  height: number;
  hasGps: boolean;
  latitude: number;
  longitude: number;
  city: string;
  country: string;
}

export interface GetFileInfo {
  path: string;
}

export interface SocialPublications {
  publications: SocialPublication[];
  since?: Date | undefined;
}

export interface Event {
  uuid: string;
  dt?: Date | undefined;
  type: string;
  content: string;
}

export interface Events {
  events: Event[];
  since?: Date | undefined;
}

export interface GetFriendshipStatus {
  domain: string;
  secret: string;
}

export interface FriendshipStatus {
  status: FriendShipStatus;
}

export interface AuthAsFriend {
  domain: string;
  secret: string;
}

export interface ReqEnvelope {
  id: number;
  payload?:
    | { $case: "reqListFiles"; reqListFiles: ListFiles }
    | { $case: "reqGetStatus"; reqGetStatus: GetStatus }
    | { $case: "reqAuth"; reqAuth: Auth }
    | { $case: "reqUploadFile"; reqUploadFile: UploadFile }
    | { $case: "reqGetFile"; reqGetFile: GetFile }
    | { $case: "reqDelFile"; reqDelFile: DelFile }
    | { $case: "reqSearchPhotos"; reqSearchPhotos: SearchPhotos }
    | { $case: "reqGetTags"; reqGetTags: GetTags }
    | { $case: "reqChangeKey"; reqChangeKey: ChangeKey }
    | { $case: "reqGetSettings"; reqGetSettings: GetSettings }
    | { $case: "reqSetSettings"; reqSetSettings: SetSettings }
    | { $case: "reqNewSocialPublication"; reqNewSocialPublication: NewSocialPublication }
    | { $case: "reqGetSocialPublications"; reqGetSocialPublications: GetSocialPublications }
    | { $case: "reqGetSocialPublicationFiles"; reqGetSocialPublicationFiles: GetSocialPublicationFiles }
    | { $case: "reqFriendshipRequest"; reqFriendshipRequest: FriendshipRequest }
    | { $case: "reqFriendshipInterRequest"; reqFriendshipInterRequest: FriendshipInterRequest }
    | { $case: "reqDidSendFriendshipReq"; reqDidSendFriendshipReq: DidSendFriendshipReq }
    | { $case: "reqFriendshipsList"; reqFriendshipsList: FriendshipsList }
    | { $case: "reqChangeFriendStatus"; reqChangeFriendStatus: ChangeFriendStatus }
    | { $case: "reqGetFriendshipStatus"; reqGetFriendshipStatus: GetFriendshipStatus }
    | { $case: "reqAuthAsFriend"; reqAuthAsFriend: AuthAsFriend }
    | { $case: "reqNewSocialComment"; reqNewSocialComment: NewSocialComment }
    | { $case: "reqGetEvents"; reqGetEvents: GetEvents }
    | { $case: "reqDelSocialComment"; reqDelSocialComment: DelSocialComment }
    | { $case: "reqLikePublication"; reqLikePublication: LikePublication }
    | { $case: "reqLikeComment"; reqLikeComment: LikeComment }
    | { $case: "reqBridgeRegister"; reqBridgeRegister: BridgeRegister }
    | { $case: "reqGetProfile"; reqGetProfile: GetProfile }
    | { $case: "reqSetProfile"; reqSetProfile: Profile }
    | { $case: "reqShareFilesLink"; reqShareFilesLink: ShareFilesLink }
    | { $case: "reqDownloadSharedLink"; reqDownloadSharedLink: DownloadSharedLink }
    | { $case: "reqGetPubKey"; reqGetPubKey: GetPubKey }
    | { $case: "reqGetPublicationLikers"; reqGetPublicationLikers: GetPublicationLikers }
    | { $case: "reqGetCommentLikers"; reqGetCommentLikers: GetCommentLikers }
    | { $case: "reqDelSocialPublication"; reqDelSocialPublication: DelSocialPublication }
    | { $case: "reqGetFileInfo"; reqGetFileInfo: GetFileInfo }
    | { $case: "reqSetBridgeSecret"; reqSetBridgeSecret: SetBridgeSecret }
    | { $case: "reqListStorageDevices"; reqListStorageDevices: ListStorageDevices }
    | { $case: "reqSetupStorage"; reqSetupStorage: SetupStorage }
    | { $case: "reqRegenerateBridgeSecret"; reqRegenerateBridgeSecret: RegenerateBridgeSecret }
    | { $case: "reqRotateBridgeSecret"; reqRotateBridgeSecret: RotateBridgeSecret }
    | { $case: "reqListWifiNetworks"; reqListWifiNetworks: ListWifiNetworks }
    | { $case: "reqSetWifi"; reqSetWifi: SetWifi }
    | undefined;
}

export interface RespEnvelope {
  id: number;
  error: boolean;
  errorMessage: string;
  payload?:
    | { $case: "respStatus"; respStatus: Status }
    | { $case: "respAck"; respAck: Ack }
    | { $case: "respFile"; respFile: File }
    | { $case: "respListOfFiles"; respListOfFiles: ListOfFiles }
    | { $case: "respTagsList"; respTagsList: TagsList }
    | { $case: "respSettings"; respSettings: Settings }
    | { $case: "respBridgeAckOnboard"; respBridgeAckOnboard: BridgeAckOnboard }
    | { $case: "respProfile"; respProfile: Profile }
    | { $case: "respShareLink"; respShareLink: ShareLink }
    | { $case: "respFriendships"; respFriendships: Friendships }
    | { $case: "respSharedFiles"; respSharedFiles: SharedFiles }
    | { $case: "respNewSocial"; respNewSocial: NewSocial }
    | { $case: "respSocialPublications"; respSocialPublications: SocialPublications }
    | { $case: "respSocialPublicationFiles"; respSocialPublicationFiles: SocialPublicationFiles }
    | { $case: "respFriendshipStatus"; respFriendshipStatus: FriendshipStatus }
    | { $case: "respEvents"; respEvents: Events }
    | { $case: "respPubKey"; respPubKey: PubKey }
    | { $case: "respLikers"; respLikers: Likers }
    | { $case: "respFileInfo"; respFileInfo: FileExifInfo }
    | { $case: "respStorageDevices"; respStorageDevices: StorageDevices }
    | { $case: "respRotateBridgeSecretAck"; respRotateBridgeSecretAck: RotateBridgeSecretAck }
    | { $case: "respWifiNetworks"; respWifiNetworks: WifiNetworks }
    | undefined;
}

function createBaseStatusErrors(): StatusErrors {
  return { StatusErrorCode: 0, Message: "" };
}

export const StatusErrors: MessageFns<StatusErrors> = {
  encode(message: StatusErrors, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.StatusErrorCode !== 0) {
      writer.uint32(8).int32(message.StatusErrorCode);
    }
    if (message.Message !== "") {
      writer.uint32(18).string(message.Message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StatusErrors {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStatusErrors();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.StatusErrorCode = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.Message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StatusErrors {
    return {
      StatusErrorCode: isSet(object.StatusErrorCode) ? statusErrorCodeFromJSON(object.StatusErrorCode) : 0,
      Message: isSet(object.Message) ? globalThis.String(object.Message) : "",
    };
  },

  toJSON(message: StatusErrors): unknown {
    const obj: any = {};
    if (message.StatusErrorCode !== 0) {
      obj.StatusErrorCode = statusErrorCodeToJSON(message.StatusErrorCode);
    }
    if (message.Message !== "") {
      obj.Message = message.Message;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StatusErrors>, I>>(base?: I): StatusErrors {
    return StatusErrors.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StatusErrors>, I>>(object: I): StatusErrors {
    const message = createBaseStatusErrors();
    message.StatusErrorCode = object.StatusErrorCode ?? 0;
    message.Message = object.Message ?? "";
    return message;
  },
};

function createBaseGetStatus(): GetStatus {
  return {};
}

export const GetStatus: MessageFns<GetStatus> = {
  encode(_: GetStatus, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetStatus {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetStatus();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): GetStatus {
    return {};
  },

  toJSON(_: GetStatus): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<GetStatus>, I>>(base?: I): GetStatus {
    return GetStatus.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetStatus>, I>>(_: I): GetStatus {
    const message = createBaseGetStatus();
    return message;
  },
};

function createBaseStatus(): Status {
  return {
    online: false,
    localIp: "",
    disks: 0,
    errors: [],
    raidSize: 0,
    raidUsage: 0,
    diskSize: 0,
    diskUsage: 0,
    cpuUsagePrc: 0,
    memSize: 0,
    memUsage: 0,
  };
}

export const Status: MessageFns<Status> = {
  encode(message: Status, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.online !== false) {
      writer.uint32(8).bool(message.online);
    }
    if (message.localIp !== "") {
      writer.uint32(18).string(message.localIp);
    }
    if (message.disks !== 0) {
      writer.uint32(24).int32(message.disks);
    }
    for (const v of message.errors) {
      StatusErrors.encode(v!, writer.uint32(34).fork()).join();
    }
    if (message.raidSize !== 0) {
      writer.uint32(40).int32(message.raidSize);
    }
    if (message.raidUsage !== 0) {
      writer.uint32(48).int32(message.raidUsage);
    }
    if (message.diskSize !== 0) {
      writer.uint32(56).int32(message.diskSize);
    }
    if (message.diskUsage !== 0) {
      writer.uint32(64).int32(message.diskUsage);
    }
    if (message.cpuUsagePrc !== 0) {
      writer.uint32(77).float(message.cpuUsagePrc);
    }
    if (message.memSize !== 0) {
      writer.uint32(80).int32(message.memSize);
    }
    if (message.memUsage !== 0) {
      writer.uint32(88).int32(message.memUsage);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Status {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStatus();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.online = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.localIp = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.disks = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.errors.push(StatusErrors.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.raidSize = reader.int32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.raidUsage = reader.int32();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.diskSize = reader.int32();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.diskUsage = reader.int32();
          continue;
        }
        case 9: {
          if (tag !== 77) {
            break;
          }

          message.cpuUsagePrc = reader.float();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.memSize = reader.int32();
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.memUsage = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Status {
    return {
      online: isSet(object.online) ? globalThis.Boolean(object.online) : false,
      localIp: isSet(object.localIp) ? globalThis.String(object.localIp) : "",
      disks: isSet(object.disks) ? globalThis.Number(object.disks) : 0,
      errors: globalThis.Array.isArray(object?.errors) ? object.errors.map((e: any) => StatusErrors.fromJSON(e)) : [],
      raidSize: isSet(object.raidSize) ? globalThis.Number(object.raidSize) : 0,
      raidUsage: isSet(object.raidUsage) ? globalThis.Number(object.raidUsage) : 0,
      diskSize: isSet(object.diskSize) ? globalThis.Number(object.diskSize) : 0,
      diskUsage: isSet(object.diskUsage) ? globalThis.Number(object.diskUsage) : 0,
      cpuUsagePrc: isSet(object.cpuUsagePrc) ? globalThis.Number(object.cpuUsagePrc) : 0,
      memSize: isSet(object.memSize) ? globalThis.Number(object.memSize) : 0,
      memUsage: isSet(object.memUsage) ? globalThis.Number(object.memUsage) : 0,
    };
  },

  toJSON(message: Status): unknown {
    const obj: any = {};
    if (message.online !== false) {
      obj.online = message.online;
    }
    if (message.localIp !== "") {
      obj.localIp = message.localIp;
    }
    if (message.disks !== 0) {
      obj.disks = Math.round(message.disks);
    }
    if (message.errors?.length) {
      obj.errors = message.errors.map((e) => StatusErrors.toJSON(e));
    }
    if (message.raidSize !== 0) {
      obj.raidSize = Math.round(message.raidSize);
    }
    if (message.raidUsage !== 0) {
      obj.raidUsage = Math.round(message.raidUsage);
    }
    if (message.diskSize !== 0) {
      obj.diskSize = Math.round(message.diskSize);
    }
    if (message.diskUsage !== 0) {
      obj.diskUsage = Math.round(message.diskUsage);
    }
    if (message.cpuUsagePrc !== 0) {
      obj.cpuUsagePrc = message.cpuUsagePrc;
    }
    if (message.memSize !== 0) {
      obj.memSize = Math.round(message.memSize);
    }
    if (message.memUsage !== 0) {
      obj.memUsage = Math.round(message.memUsage);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Status>, I>>(base?: I): Status {
    return Status.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Status>, I>>(object: I): Status {
    const message = createBaseStatus();
    message.online = object.online ?? false;
    message.localIp = object.localIp ?? "";
    message.disks = object.disks ?? 0;
    message.errors = object.errors?.map((e) => StatusErrors.fromPartial(e)) || [];
    message.raidSize = object.raidSize ?? 0;
    message.raidUsage = object.raidUsage ?? 0;
    message.diskSize = object.diskSize ?? 0;
    message.diskUsage = object.diskUsage ?? 0;
    message.cpuUsagePrc = object.cpuUsagePrc ?? 0;
    message.memSize = object.memSize ?? 0;
    message.memUsage = object.memUsage ?? 0;
    return message;
  },
};

function createBaseAuth(): Auth {
  return { uuid: "", key: new Uint8Array(0), create: false };
}

export const Auth: MessageFns<Auth> = {
  encode(message: Auth, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.uuid !== "") {
      writer.uint32(10).string(message.uuid);
    }
    if (message.key.length !== 0) {
      writer.uint32(18).bytes(message.key);
    }
    if (message.create !== false) {
      writer.uint32(24).bool(message.create);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Auth {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAuth();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.uuid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.key = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.create = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Auth {
    return {
      uuid: isSet(object.uuid) ? globalThis.String(object.uuid) : "",
      key: isSet(object.key) ? bytesFromBase64(object.key) : new Uint8Array(0),
      create: isSet(object.create) ? globalThis.Boolean(object.create) : false,
    };
  },

  toJSON(message: Auth): unknown {
    const obj: any = {};
    if (message.uuid !== "") {
      obj.uuid = message.uuid;
    }
    if (message.key.length !== 0) {
      obj.key = base64FromBytes(message.key);
    }
    if (message.create !== false) {
      obj.create = message.create;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Auth>, I>>(base?: I): Auth {
    return Auth.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Auth>, I>>(object: I): Auth {
    const message = createBaseAuth();
    message.uuid = object.uuid ?? "";
    message.key = object.key ?? new Uint8Array(0);
    message.create = object.create ?? false;
    return message;
  },
};

function createBaseGetPubKey(): GetPubKey {
  return {};
}

export const GetPubKey: MessageFns<GetPubKey> = {
  encode(_: GetPubKey, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetPubKey {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetPubKey();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): GetPubKey {
    return {};
  },

  toJSON(_: GetPubKey): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<GetPubKey>, I>>(base?: I): GetPubKey {
    return GetPubKey.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetPubKey>, I>>(_: I): GetPubKey {
    const message = createBaseGetPubKey();
    return message;
  },
};

function createBasePubKey(): PubKey {
  return { publicKey: new Uint8Array(0), isNewDevice: false };
}

export const PubKey: MessageFns<PubKey> = {
  encode(message: PubKey, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.publicKey.length !== 0) {
      writer.uint32(10).bytes(message.publicKey);
    }
    if (message.isNewDevice !== false) {
      writer.uint32(16).bool(message.isNewDevice);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PubKey {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePubKey();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.publicKey = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.isNewDevice = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PubKey {
    return {
      publicKey: isSet(object.publicKey) ? bytesFromBase64(object.publicKey) : new Uint8Array(0),
      isNewDevice: isSet(object.isNewDevice) ? globalThis.Boolean(object.isNewDevice) : false,
    };
  },

  toJSON(message: PubKey): unknown {
    const obj: any = {};
    if (message.publicKey.length !== 0) {
      obj.publicKey = base64FromBytes(message.publicKey);
    }
    if (message.isNewDevice !== false) {
      obj.isNewDevice = message.isNewDevice;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PubKey>, I>>(base?: I): PubKey {
    return PubKey.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PubKey>, I>>(object: I): PubKey {
    const message = createBasePubKey();
    message.publicKey = object.publicKey ?? new Uint8Array(0);
    message.isNewDevice = object.isNewDevice ?? false;
    return message;
  },
};

function createBaseUploadFile(): UploadFile {
  return { path: "", content: new Uint8Array(0), forceOverride: false, created: undefined };
}

export const UploadFile: MessageFns<UploadFile> = {
  encode(message: UploadFile, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.path !== "") {
      writer.uint32(10).string(message.path);
    }
    if (message.content.length !== 0) {
      writer.uint32(18).bytes(message.content);
    }
    if (message.forceOverride !== false) {
      writer.uint32(24).bool(message.forceOverride);
    }
    if (message.created !== undefined) {
      Timestamp.encode(toTimestamp(message.created), writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UploadFile {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUploadFile();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.path = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.content = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.forceOverride = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.created = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UploadFile {
    return {
      path: isSet(object.path) ? globalThis.String(object.path) : "",
      content: isSet(object.content) ? bytesFromBase64(object.content) : new Uint8Array(0),
      forceOverride: isSet(object.forceOverride) ? globalThis.Boolean(object.forceOverride) : false,
      created: isSet(object.created) ? fromJsonTimestamp(object.created) : undefined,
    };
  },

  toJSON(message: UploadFile): unknown {
    const obj: any = {};
    if (message.path !== "") {
      obj.path = message.path;
    }
    if (message.content.length !== 0) {
      obj.content = base64FromBytes(message.content);
    }
    if (message.forceOverride !== false) {
      obj.forceOverride = message.forceOverride;
    }
    if (message.created !== undefined) {
      obj.created = message.created.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UploadFile>, I>>(base?: I): UploadFile {
    return UploadFile.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UploadFile>, I>>(object: I): UploadFile {
    const message = createBaseUploadFile();
    message.path = object.path ?? "";
    message.content = object.content ?? new Uint8Array(0);
    message.forceOverride = object.forceOverride ?? false;
    message.created = object.created ?? undefined;
    return message;
  },
};

function createBaseDelFile(): DelFile {
  return { path: "" };
}

export const DelFile: MessageFns<DelFile> = {
  encode(message: DelFile, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.path !== "") {
      writer.uint32(10).string(message.path);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DelFile {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDelFile();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.path = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DelFile {
    return { path: isSet(object.path) ? globalThis.String(object.path) : "" };
  },

  toJSON(message: DelFile): unknown {
    const obj: any = {};
    if (message.path !== "") {
      obj.path = message.path;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DelFile>, I>>(base?: I): DelFile {
    return DelFile.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DelFile>, I>>(object: I): DelFile {
    const message = createBaseDelFile();
    message.path = object.path ?? "";
    return message;
  },
};

function createBaseGetFile(): GetFile {
  return { path: "" };
}

export const GetFile: MessageFns<GetFile> = {
  encode(message: GetFile, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.path !== "") {
      writer.uint32(10).string(message.path);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetFile {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetFile();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.path = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetFile {
    return { path: isSet(object.path) ? globalThis.String(object.path) : "" };
  },

  toJSON(message: GetFile): unknown {
    const obj: any = {};
    if (message.path !== "") {
      obj.path = message.path;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetFile>, I>>(base?: I): GetFile {
    return GetFile.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetFile>, I>>(object: I): GetFile {
    const message = createBaseGetFile();
    message.path = object.path ?? "";
    return message;
  },
};

function createBaseListFiles(): ListFiles {
  return { path: "", recursive: false };
}

export const ListFiles: MessageFns<ListFiles> = {
  encode(message: ListFiles, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.path !== "") {
      writer.uint32(10).string(message.path);
    }
    if (message.recursive !== false) {
      writer.uint32(16).bool(message.recursive);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListFiles {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListFiles();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.path = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.recursive = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListFiles {
    return {
      path: isSet(object.path) ? globalThis.String(object.path) : "",
      recursive: isSet(object.recursive) ? globalThis.Boolean(object.recursive) : false,
    };
  },

  toJSON(message: ListFiles): unknown {
    const obj: any = {};
    if (message.path !== "") {
      obj.path = message.path;
    }
    if (message.recursive !== false) {
      obj.recursive = message.recursive;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListFiles>, I>>(base?: I): ListFiles {
    return ListFiles.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListFiles>, I>>(object: I): ListFiles {
    const message = createBaseListFiles();
    message.path = object.path ?? "";
    message.recursive = object.recursive ?? false;
    return message;
  },
};

function createBaseSearchPhotos(): SearchPhotos {
  return { tags: [], token: "" };
}

export const SearchPhotos: MessageFns<SearchPhotos> = {
  encode(message: SearchPhotos, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.tags) {
      writer.uint32(10).string(v!);
    }
    if (message.token !== "") {
      writer.uint32(18).string(message.token);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SearchPhotos {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSearchPhotos();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.tags.push(reader.string());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.token = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SearchPhotos {
    return {
      tags: globalThis.Array.isArray(object?.tags) ? object.tags.map((e: any) => globalThis.String(e)) : [],
      token: isSet(object.token) ? globalThis.String(object.token) : "",
    };
  },

  toJSON(message: SearchPhotos): unknown {
    const obj: any = {};
    if (message.tags?.length) {
      obj.tags = message.tags;
    }
    if (message.token !== "") {
      obj.token = message.token;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SearchPhotos>, I>>(base?: I): SearchPhotos {
    return SearchPhotos.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SearchPhotos>, I>>(object: I): SearchPhotos {
    const message = createBaseSearchPhotos();
    message.tags = object.tags?.map((e) => e) || [];
    message.token = object.token ?? "";
    return message;
  },
};

function createBaseListOfFiles(): ListOfFiles {
  return { files: [], token: "" };
}

export const ListOfFiles: MessageFns<ListOfFiles> = {
  encode(message: ListOfFiles, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.files) {
      File.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.token !== "") {
      writer.uint32(18).string(message.token);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListOfFiles {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListOfFiles();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.files.push(File.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.token = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListOfFiles {
    return {
      files: globalThis.Array.isArray(object?.files) ? object.files.map((e: any) => File.fromJSON(e)) : [],
      token: isSet(object.token) ? globalThis.String(object.token) : "",
    };
  },

  toJSON(message: ListOfFiles): unknown {
    const obj: any = {};
    if (message.files?.length) {
      obj.files = message.files.map((e) => File.toJSON(e));
    }
    if (message.token !== "") {
      obj.token = message.token;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListOfFiles>, I>>(base?: I): ListOfFiles {
    return ListOfFiles.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListOfFiles>, I>>(object: I): ListOfFiles {
    const message = createBaseListOfFiles();
    message.files = object.files?.map((e) => File.fromPartial(e)) || [];
    message.token = object.token ?? "";
    return message;
  },
};

function createBaseFile(): File {
  return { hash: "", mime: "", created: undefined, modified: undefined, path: "", size: 0, content: undefined };
}

export const File: MessageFns<File> = {
  encode(message: File, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.hash !== "") {
      writer.uint32(10).string(message.hash);
    }
    if (message.mime !== "") {
      writer.uint32(18).string(message.mime);
    }
    if (message.created !== undefined) {
      Timestamp.encode(toTimestamp(message.created), writer.uint32(26).fork()).join();
    }
    if (message.modified !== undefined) {
      Timestamp.encode(toTimestamp(message.modified), writer.uint32(34).fork()).join();
    }
    if (message.path !== "") {
      writer.uint32(42).string(message.path);
    }
    if (message.size !== 0) {
      writer.uint32(48).int32(message.size);
    }
    if (message.content !== undefined) {
      writer.uint32(66).bytes(message.content);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): File {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFile();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.hash = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.mime = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.created = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.modified = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.path = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.size = reader.int32();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.content = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): File {
    return {
      hash: isSet(object.hash) ? globalThis.String(object.hash) : "",
      mime: isSet(object.mime) ? globalThis.String(object.mime) : "",
      created: isSet(object.created) ? fromJsonTimestamp(object.created) : undefined,
      modified: isSet(object.modified) ? fromJsonTimestamp(object.modified) : undefined,
      path: isSet(object.path) ? globalThis.String(object.path) : "",
      size: isSet(object.size) ? globalThis.Number(object.size) : 0,
      content: isSet(object.content) ? bytesFromBase64(object.content) : undefined,
    };
  },

  toJSON(message: File): unknown {
    const obj: any = {};
    if (message.hash !== "") {
      obj.hash = message.hash;
    }
    if (message.mime !== "") {
      obj.mime = message.mime;
    }
    if (message.created !== undefined) {
      obj.created = message.created.toISOString();
    }
    if (message.modified !== undefined) {
      obj.modified = message.modified.toISOString();
    }
    if (message.path !== "") {
      obj.path = message.path;
    }
    if (message.size !== 0) {
      obj.size = Math.round(message.size);
    }
    if (message.content !== undefined) {
      obj.content = base64FromBytes(message.content);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<File>, I>>(base?: I): File {
    return File.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<File>, I>>(object: I): File {
    const message = createBaseFile();
    message.hash = object.hash ?? "";
    message.mime = object.mime ?? "";
    message.created = object.created ?? undefined;
    message.modified = object.modified ?? undefined;
    message.path = object.path ?? "";
    message.size = object.size ?? 0;
    message.content = object.content ?? undefined;
    return message;
  },
};

function createBaseAck(): Ack {
  return { ok: false, errorMsg: "" };
}

export const Ack: MessageFns<Ack> = {
  encode(message: Ack, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ok !== false) {
      writer.uint32(8).bool(message.ok);
    }
    if (message.errorMsg !== "") {
      writer.uint32(18).string(message.errorMsg);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Ack {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAck();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.ok = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.errorMsg = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Ack {
    return {
      ok: isSet(object.ok) ? globalThis.Boolean(object.ok) : false,
      errorMsg: isSet(object.errorMsg) ? globalThis.String(object.errorMsg) : "",
    };
  },

  toJSON(message: Ack): unknown {
    const obj: any = {};
    if (message.ok !== false) {
      obj.ok = message.ok;
    }
    if (message.errorMsg !== "") {
      obj.errorMsg = message.errorMsg;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Ack>, I>>(base?: I): Ack {
    return Ack.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Ack>, I>>(object: I): Ack {
    const message = createBaseAck();
    message.ok = object.ok ?? false;
    message.errorMsg = object.errorMsg ?? "";
    return message;
  },
};

function createBaseGetTags(): GetTags {
  return {};
}

export const GetTags: MessageFns<GetTags> = {
  encode(_: GetTags, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetTags {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetTags();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): GetTags {
    return {};
  },

  toJSON(_: GetTags): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<GetTags>, I>>(base?: I): GetTags {
    return GetTags.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetTags>, I>>(_: I): GetTags {
    const message = createBaseGetTags();
    return message;
  },
};

function createBaseTagsList(): TagsList {
  return { tags: [] };
}

export const TagsList: MessageFns<TagsList> = {
  encode(message: TagsList, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.tags) {
      writer.uint32(10).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TagsList {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTagsList();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.tags.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TagsList {
    return { tags: globalThis.Array.isArray(object?.tags) ? object.tags.map((e: any) => globalThis.String(e)) : [] };
  },

  toJSON(message: TagsList): unknown {
    const obj: any = {};
    if (message.tags?.length) {
      obj.tags = message.tags;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TagsList>, I>>(base?: I): TagsList {
    return TagsList.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TagsList>, I>>(object: I): TagsList {
    const message = createBaseTagsList();
    message.tags = object.tags?.map((e) => e) || [];
    return message;
  },
};

function createBaseChangeKey(): ChangeKey {
  return { oldKey: new Uint8Array(0), newKey: new Uint8Array(0) };
}

export const ChangeKey: MessageFns<ChangeKey> = {
  encode(message: ChangeKey, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.oldKey.length !== 0) {
      writer.uint32(10).bytes(message.oldKey);
    }
    if (message.newKey.length !== 0) {
      writer.uint32(18).bytes(message.newKey);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ChangeKey {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseChangeKey();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.oldKey = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.newKey = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ChangeKey {
    return {
      oldKey: isSet(object.oldKey) ? bytesFromBase64(object.oldKey) : new Uint8Array(0),
      newKey: isSet(object.newKey) ? bytesFromBase64(object.newKey) : new Uint8Array(0),
    };
  },

  toJSON(message: ChangeKey): unknown {
    const obj: any = {};
    if (message.oldKey.length !== 0) {
      obj.oldKey = base64FromBytes(message.oldKey);
    }
    if (message.newKey.length !== 0) {
      obj.newKey = base64FromBytes(message.newKey);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ChangeKey>, I>>(base?: I): ChangeKey {
    return ChangeKey.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ChangeKey>, I>>(object: I): ChangeKey {
    const message = createBaseChangeKey();
    message.oldKey = object.oldKey ?? new Uint8Array(0);
    message.newKey = object.newKey ?? new Uint8Array(0);
    return message;
  },
};

function createBaseNewSocialPublication(): NewSocialPublication {
  return { text: "", paths: [] };
}

export const NewSocialPublication: MessageFns<NewSocialPublication> = {
  encode(message: NewSocialPublication, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.text !== "") {
      writer.uint32(10).string(message.text);
    }
    for (const v of message.paths) {
      writer.uint32(18).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NewSocialPublication {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNewSocialPublication();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.text = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.paths.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): NewSocialPublication {
    return {
      text: isSet(object.text) ? globalThis.String(object.text) : "",
      paths: globalThis.Array.isArray(object?.paths) ? object.paths.map((e: any) => globalThis.String(e)) : [],
    };
  },

  toJSON(message: NewSocialPublication): unknown {
    const obj: any = {};
    if (message.text !== "") {
      obj.text = message.text;
    }
    if (message.paths?.length) {
      obj.paths = message.paths;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<NewSocialPublication>, I>>(base?: I): NewSocialPublication {
    return NewSocialPublication.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<NewSocialPublication>, I>>(object: I): NewSocialPublication {
    const message = createBaseNewSocialPublication();
    message.text = object.text ?? "";
    message.paths = object.paths?.map((e) => e) || [];
    return message;
  },
};

function createBaseGetEvents(): GetEvents {
  return { since: undefined, total: 0 };
}

export const GetEvents: MessageFns<GetEvents> = {
  encode(message: GetEvents, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.since !== undefined) {
      Timestamp.encode(toTimestamp(message.since), writer.uint32(10).fork()).join();
    }
    if (message.total !== 0) {
      writer.uint32(16).int32(message.total);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetEvents {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetEvents();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.since = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.total = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetEvents {
    return {
      since: isSet(object.since) ? fromJsonTimestamp(object.since) : undefined,
      total: isSet(object.total) ? globalThis.Number(object.total) : 0,
    };
  },

  toJSON(message: GetEvents): unknown {
    const obj: any = {};
    if (message.since !== undefined) {
      obj.since = message.since.toISOString();
    }
    if (message.total !== 0) {
      obj.total = Math.round(message.total);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetEvents>, I>>(base?: I): GetEvents {
    return GetEvents.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetEvents>, I>>(object: I): GetEvents {
    const message = createBaseGetEvents();
    message.since = object.since ?? undefined;
    message.total = object.total ?? 0;
    return message;
  },
};

function createBaseGetSocialPublicationFiles(): GetSocialPublicationFiles {
  return { uuid: "" };
}

export const GetSocialPublicationFiles: MessageFns<GetSocialPublicationFiles> = {
  encode(message: GetSocialPublicationFiles, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.uuid !== "") {
      writer.uint32(10).string(message.uuid);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetSocialPublicationFiles {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetSocialPublicationFiles();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.uuid = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetSocialPublicationFiles {
    return { uuid: isSet(object.uuid) ? globalThis.String(object.uuid) : "" };
  },

  toJSON(message: GetSocialPublicationFiles): unknown {
    const obj: any = {};
    if (message.uuid !== "") {
      obj.uuid = message.uuid;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetSocialPublicationFiles>, I>>(base?: I): GetSocialPublicationFiles {
    return GetSocialPublicationFiles.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetSocialPublicationFiles>, I>>(object: I): GetSocialPublicationFiles {
    const message = createBaseGetSocialPublicationFiles();
    message.uuid = object.uuid ?? "";
    return message;
  },
};

function createBaseGetSocialPublications(): GetSocialPublications {
  return { since: undefined, total: 0, excludeUuids: [] };
}

export const GetSocialPublications: MessageFns<GetSocialPublications> = {
  encode(message: GetSocialPublications, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.since !== undefined) {
      Timestamp.encode(toTimestamp(message.since), writer.uint32(10).fork()).join();
    }
    if (message.total !== 0) {
      writer.uint32(16).int32(message.total);
    }
    for (const v of message.excludeUuids) {
      writer.uint32(26).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetSocialPublications {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetSocialPublications();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.since = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.total = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.excludeUuids.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetSocialPublications {
    return {
      since: isSet(object.since) ? fromJsonTimestamp(object.since) : undefined,
      total: isSet(object.total) ? globalThis.Number(object.total) : 0,
      excludeUuids: globalThis.Array.isArray(object?.excludeUuids)
        ? object.excludeUuids.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: GetSocialPublications): unknown {
    const obj: any = {};
    if (message.since !== undefined) {
      obj.since = message.since.toISOString();
    }
    if (message.total !== 0) {
      obj.total = Math.round(message.total);
    }
    if (message.excludeUuids?.length) {
      obj.excludeUuids = message.excludeUuids;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetSocialPublications>, I>>(base?: I): GetSocialPublications {
    return GetSocialPublications.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetSocialPublications>, I>>(object: I): GetSocialPublications {
    const message = createBaseGetSocialPublications();
    message.since = object.since ?? undefined;
    message.total = object.total ?? 0;
    message.excludeUuids = object.excludeUuids?.map((e) => e) || [];
    return message;
  },
};

function createBaseNewSocialComment(): NewSocialComment {
  return { pubUuid: "", comment: "", publisher: "" };
}

export const NewSocialComment: MessageFns<NewSocialComment> = {
  encode(message: NewSocialComment, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.pubUuid !== "") {
      writer.uint32(10).string(message.pubUuid);
    }
    if (message.comment !== "") {
      writer.uint32(18).string(message.comment);
    }
    if (message.publisher !== "") {
      writer.uint32(26).string(message.publisher);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NewSocialComment {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNewSocialComment();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.pubUuid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.comment = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.publisher = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): NewSocialComment {
    return {
      pubUuid: isSet(object.pubUuid) ? globalThis.String(object.pubUuid) : "",
      comment: isSet(object.comment) ? globalThis.String(object.comment) : "",
      publisher: isSet(object.publisher) ? globalThis.String(object.publisher) : "",
    };
  },

  toJSON(message: NewSocialComment): unknown {
    const obj: any = {};
    if (message.pubUuid !== "") {
      obj.pubUuid = message.pubUuid;
    }
    if (message.comment !== "") {
      obj.comment = message.comment;
    }
    if (message.publisher !== "") {
      obj.publisher = message.publisher;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<NewSocialComment>, I>>(base?: I): NewSocialComment {
    return NewSocialComment.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<NewSocialComment>, I>>(object: I): NewSocialComment {
    const message = createBaseNewSocialComment();
    message.pubUuid = object.pubUuid ?? "";
    message.comment = object.comment ?? "";
    message.publisher = object.publisher ?? "";
    return message;
  },
};

function createBaseDelSocialComment(): DelSocialComment {
  return { commentUuid: "" };
}

export const DelSocialComment: MessageFns<DelSocialComment> = {
  encode(message: DelSocialComment, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.commentUuid !== "") {
      writer.uint32(10).string(message.commentUuid);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DelSocialComment {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDelSocialComment();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.commentUuid = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DelSocialComment {
    return { commentUuid: isSet(object.commentUuid) ? globalThis.String(object.commentUuid) : "" };
  },

  toJSON(message: DelSocialComment): unknown {
    const obj: any = {};
    if (message.commentUuid !== "") {
      obj.commentUuid = message.commentUuid;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DelSocialComment>, I>>(base?: I): DelSocialComment {
    return DelSocialComment.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DelSocialComment>, I>>(object: I): DelSocialComment {
    const message = createBaseDelSocialComment();
    message.commentUuid = object.commentUuid ?? "";
    return message;
  },
};

function createBaseDelSocialPublication(): DelSocialPublication {
  return { pubUuid: "" };
}

export const DelSocialPublication: MessageFns<DelSocialPublication> = {
  encode(message: DelSocialPublication, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.pubUuid !== "") {
      writer.uint32(10).string(message.pubUuid);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DelSocialPublication {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDelSocialPublication();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.pubUuid = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DelSocialPublication {
    return { pubUuid: isSet(object.pubUuid) ? globalThis.String(object.pubUuid) : "" };
  },

  toJSON(message: DelSocialPublication): unknown {
    const obj: any = {};
    if (message.pubUuid !== "") {
      obj.pubUuid = message.pubUuid;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DelSocialPublication>, I>>(base?: I): DelSocialPublication {
    return DelSocialPublication.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DelSocialPublication>, I>>(object: I): DelSocialPublication {
    const message = createBaseDelSocialPublication();
    message.pubUuid = object.pubUuid ?? "";
    return message;
  },
};

function createBaseDidSendFriendshipReq(): DidSendFriendshipReq {
  return { secret: "", domain: "" };
}

export const DidSendFriendshipReq: MessageFns<DidSendFriendshipReq> = {
  encode(message: DidSendFriendshipReq, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.secret !== "") {
      writer.uint32(10).string(message.secret);
    }
    if (message.domain !== "") {
      writer.uint32(18).string(message.domain);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DidSendFriendshipReq {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDidSendFriendshipReq();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.secret = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.domain = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DidSendFriendshipReq {
    return {
      secret: isSet(object.secret) ? globalThis.String(object.secret) : "",
      domain: isSet(object.domain) ? globalThis.String(object.domain) : "",
    };
  },

  toJSON(message: DidSendFriendshipReq): unknown {
    const obj: any = {};
    if (message.secret !== "") {
      obj.secret = message.secret;
    }
    if (message.domain !== "") {
      obj.domain = message.domain;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DidSendFriendshipReq>, I>>(base?: I): DidSendFriendshipReq {
    return DidSendFriendshipReq.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DidSendFriendshipReq>, I>>(object: I): DidSendFriendshipReq {
    const message = createBaseDidSendFriendshipReq();
    message.secret = object.secret ?? "";
    message.domain = object.domain ?? "";
    return message;
  },
};

function createBaseFriendship(): Friendship {
  return { originProfile: undefined, status: 0, sent: false, secret: "", latestSync: undefined };
}

export const Friendship: MessageFns<Friendship> = {
  encode(message: Friendship, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.originProfile !== undefined) {
      Profile.encode(message.originProfile, writer.uint32(18).fork()).join();
    }
    if (message.status !== 0) {
      writer.uint32(24).int32(message.status);
    }
    if (message.sent !== false) {
      writer.uint32(32).bool(message.sent);
    }
    if (message.secret !== "") {
      writer.uint32(42).string(message.secret);
    }
    if (message.latestSync !== undefined) {
      Timestamp.encode(toTimestamp(message.latestSync), writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Friendship {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFriendship();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.originProfile = Profile.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.sent = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.secret = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.latestSync = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Friendship {
    return {
      originProfile: isSet(object.originProfile) ? Profile.fromJSON(object.originProfile) : undefined,
      status: isSet(object.status) ? friendShipStatusFromJSON(object.status) : 0,
      sent: isSet(object.sent) ? globalThis.Boolean(object.sent) : false,
      secret: isSet(object.secret) ? globalThis.String(object.secret) : "",
      latestSync: isSet(object.latestSync) ? fromJsonTimestamp(object.latestSync) : undefined,
    };
  },

  toJSON(message: Friendship): unknown {
    const obj: any = {};
    if (message.originProfile !== undefined) {
      obj.originProfile = Profile.toJSON(message.originProfile);
    }
    if (message.status !== 0) {
      obj.status = friendShipStatusToJSON(message.status);
    }
    if (message.sent !== false) {
      obj.sent = message.sent;
    }
    if (message.secret !== "") {
      obj.secret = message.secret;
    }
    if (message.latestSync !== undefined) {
      obj.latestSync = message.latestSync.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Friendship>, I>>(base?: I): Friendship {
    return Friendship.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Friendship>, I>>(object: I): Friendship {
    const message = createBaseFriendship();
    message.originProfile = (object.originProfile !== undefined && object.originProfile !== null)
      ? Profile.fromPartial(object.originProfile)
      : undefined;
    message.status = object.status ?? 0;
    message.sent = object.sent ?? false;
    message.secret = object.secret ?? "";
    message.latestSync = object.latestSync ?? undefined;
    return message;
  },
};

function createBaseFriendships(): Friendships {
  return { friendships: [] };
}

export const Friendships: MessageFns<Friendships> = {
  encode(message: Friendships, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.friendships) {
      Friendship.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Friendships {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFriendships();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.friendships.push(Friendship.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Friendships {
    return {
      friendships: globalThis.Array.isArray(object?.friendships)
        ? object.friendships.map((e: any) => Friendship.fromJSON(e))
        : [],
    };
  },

  toJSON(message: Friendships): unknown {
    const obj: any = {};
    if (message.friendships?.length) {
      obj.friendships = message.friendships.map((e) => Friendship.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Friendships>, I>>(base?: I): Friendships {
    return Friendships.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Friendships>, I>>(object: I): Friendships {
    const message = createBaseFriendships();
    message.friendships = object.friendships?.map((e) => Friendship.fromPartial(e)) || [];
    return message;
  },
};

function createBaseFriendshipsList(): FriendshipsList {
  return {};
}

export const FriendshipsList: MessageFns<FriendshipsList> = {
  encode(_: FriendshipsList, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FriendshipsList {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFriendshipsList();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): FriendshipsList {
    return {};
  },

  toJSON(_: FriendshipsList): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<FriendshipsList>, I>>(base?: I): FriendshipsList {
    return FriendshipsList.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FriendshipsList>, I>>(_: I): FriendshipsList {
    const message = createBaseFriendshipsList();
    return message;
  },
};

function createBaseFriendshipRequest(): FriendshipRequest {
  return { domain: "" };
}

export const FriendshipRequest: MessageFns<FriendshipRequest> = {
  encode(message: FriendshipRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.domain !== "") {
      writer.uint32(10).string(message.domain);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FriendshipRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFriendshipRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.domain = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FriendshipRequest {
    return { domain: isSet(object.domain) ? globalThis.String(object.domain) : "" };
  },

  toJSON(message: FriendshipRequest): unknown {
    const obj: any = {};
    if (message.domain !== "") {
      obj.domain = message.domain;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FriendshipRequest>, I>>(base?: I): FriendshipRequest {
    return FriendshipRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FriendshipRequest>, I>>(object: I): FriendshipRequest {
    const message = createBaseFriendshipRequest();
    message.domain = object.domain ?? "";
    return message;
  },
};

function createBaseFriendshipInterRequest(): FriendshipInterRequest {
  return { domain: "", secret: "", originProfile: undefined };
}

export const FriendshipInterRequest: MessageFns<FriendshipInterRequest> = {
  encode(message: FriendshipInterRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.domain !== "") {
      writer.uint32(10).string(message.domain);
    }
    if (message.secret !== "") {
      writer.uint32(18).string(message.secret);
    }
    if (message.originProfile !== undefined) {
      Profile.encode(message.originProfile, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FriendshipInterRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFriendshipInterRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.domain = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.secret = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.originProfile = Profile.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FriendshipInterRequest {
    return {
      domain: isSet(object.domain) ? globalThis.String(object.domain) : "",
      secret: isSet(object.secret) ? globalThis.String(object.secret) : "",
      originProfile: isSet(object.originProfile) ? Profile.fromJSON(object.originProfile) : undefined,
    };
  },

  toJSON(message: FriendshipInterRequest): unknown {
    const obj: any = {};
    if (message.domain !== "") {
      obj.domain = message.domain;
    }
    if (message.secret !== "") {
      obj.secret = message.secret;
    }
    if (message.originProfile !== undefined) {
      obj.originProfile = Profile.toJSON(message.originProfile);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FriendshipInterRequest>, I>>(base?: I): FriendshipInterRequest {
    return FriendshipInterRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FriendshipInterRequest>, I>>(object: I): FriendshipInterRequest {
    const message = createBaseFriendshipInterRequest();
    message.domain = object.domain ?? "";
    message.secret = object.secret ?? "";
    message.originProfile = (object.originProfile !== undefined && object.originProfile !== null)
      ? Profile.fromPartial(object.originProfile)
      : undefined;
    return message;
  },
};

function createBaseChangeFriendStatus(): ChangeFriendStatus {
  return { domain: "", status: 0 };
}

export const ChangeFriendStatus: MessageFns<ChangeFriendStatus> = {
  encode(message: ChangeFriendStatus, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.domain !== "") {
      writer.uint32(10).string(message.domain);
    }
    if (message.status !== 0) {
      writer.uint32(16).int32(message.status);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ChangeFriendStatus {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseChangeFriendStatus();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.domain = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ChangeFriendStatus {
    return {
      domain: isSet(object.domain) ? globalThis.String(object.domain) : "",
      status: isSet(object.status) ? friendShipStatusFromJSON(object.status) : 0,
    };
  },

  toJSON(message: ChangeFriendStatus): unknown {
    const obj: any = {};
    if (message.domain !== "") {
      obj.domain = message.domain;
    }
    if (message.status !== 0) {
      obj.status = friendShipStatusToJSON(message.status);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ChangeFriendStatus>, I>>(base?: I): ChangeFriendStatus {
    return ChangeFriendStatus.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ChangeFriendStatus>, I>>(object: I): ChangeFriendStatus {
    const message = createBaseChangeFriendStatus();
    message.domain = object.domain ?? "";
    message.status = object.status ?? 0;
    return message;
  },
};

function createBaseLikePublication(): LikePublication {
  return { pubUuid: "" };
}

export const LikePublication: MessageFns<LikePublication> = {
  encode(message: LikePublication, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.pubUuid !== "") {
      writer.uint32(10).string(message.pubUuid);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LikePublication {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLikePublication();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.pubUuid = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LikePublication {
    return { pubUuid: isSet(object.pubUuid) ? globalThis.String(object.pubUuid) : "" };
  },

  toJSON(message: LikePublication): unknown {
    const obj: any = {};
    if (message.pubUuid !== "") {
      obj.pubUuid = message.pubUuid;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LikePublication>, I>>(base?: I): LikePublication {
    return LikePublication.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LikePublication>, I>>(object: I): LikePublication {
    const message = createBaseLikePublication();
    message.pubUuid = object.pubUuid ?? "";
    return message;
  },
};

function createBaseLikeComment(): LikeComment {
  return { commentUuid: "" };
}

export const LikeComment: MessageFns<LikeComment> = {
  encode(message: LikeComment, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.commentUuid !== "") {
      writer.uint32(10).string(message.commentUuid);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LikeComment {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLikeComment();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.commentUuid = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LikeComment {
    return { commentUuid: isSet(object.commentUuid) ? globalThis.String(object.commentUuid) : "" };
  },

  toJSON(message: LikeComment): unknown {
    const obj: any = {};
    if (message.commentUuid !== "") {
      obj.commentUuid = message.commentUuid;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LikeComment>, I>>(base?: I): LikeComment {
    return LikeComment.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LikeComment>, I>>(object: I): LikeComment {
    const message = createBaseLikeComment();
    message.commentUuid = object.commentUuid ?? "";
    return message;
  },
};

function createBaseGetPublicationLikers(): GetPublicationLikers {
  return { pubUuid: "" };
}

export const GetPublicationLikers: MessageFns<GetPublicationLikers> = {
  encode(message: GetPublicationLikers, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.pubUuid !== "") {
      writer.uint32(10).string(message.pubUuid);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetPublicationLikers {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetPublicationLikers();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.pubUuid = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetPublicationLikers {
    return { pubUuid: isSet(object.pubUuid) ? globalThis.String(object.pubUuid) : "" };
  },

  toJSON(message: GetPublicationLikers): unknown {
    const obj: any = {};
    if (message.pubUuid !== "") {
      obj.pubUuid = message.pubUuid;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetPublicationLikers>, I>>(base?: I): GetPublicationLikers {
    return GetPublicationLikers.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetPublicationLikers>, I>>(object: I): GetPublicationLikers {
    const message = createBaseGetPublicationLikers();
    message.pubUuid = object.pubUuid ?? "";
    return message;
  },
};

function createBaseGetCommentLikers(): GetCommentLikers {
  return { commentUuid: "" };
}

export const GetCommentLikers: MessageFns<GetCommentLikers> = {
  encode(message: GetCommentLikers, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.commentUuid !== "") {
      writer.uint32(10).string(message.commentUuid);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetCommentLikers {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetCommentLikers();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.commentUuid = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetCommentLikers {
    return { commentUuid: isSet(object.commentUuid) ? globalThis.String(object.commentUuid) : "" };
  },

  toJSON(message: GetCommentLikers): unknown {
    const obj: any = {};
    if (message.commentUuid !== "") {
      obj.commentUuid = message.commentUuid;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetCommentLikers>, I>>(base?: I): GetCommentLikers {
    return GetCommentLikers.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetCommentLikers>, I>>(object: I): GetCommentLikers {
    const message = createBaseGetCommentLikers();
    message.commentUuid = object.commentUuid ?? "";
    return message;
  },
};

function createBaseLikers(): Likers {
  return { likers: [] };
}

export const Likers: MessageFns<Likers> = {
  encode(message: Likers, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.likers) {
      Profile.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Likers {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLikers();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.likers.push(Profile.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Likers {
    return {
      likers: globalThis.Array.isArray(object?.likers) ? object.likers.map((e: any) => Profile.fromJSON(e)) : [],
    };
  },

  toJSON(message: Likers): unknown {
    const obj: any = {};
    if (message.likers?.length) {
      obj.likers = message.likers.map((e) => Profile.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Likers>, I>>(base?: I): Likers {
    return Likers.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Likers>, I>>(object: I): Likers {
    const message = createBaseLikers();
    message.likers = object.likers?.map((e) => Profile.fromPartial(e)) || [];
    return message;
  },
};

function createBaseGetSettings(): GetSettings {
  return {};
}

export const GetSettings: MessageFns<GetSettings> = {
  encode(_: GetSettings, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetSettings {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetSettings();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): GetSettings {
    return {};
  },

  toJSON(_: GetSettings): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<GetSettings>, I>>(base?: I): GetSettings {
    return GetSettings.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetSettings>, I>>(_: I): GetSettings {
    const message = createBaseGetSettings();
    return message;
  },
};

function createBaseSetSettings(): SetSettings {
  return { domain: "" };
}

export const SetSettings: MessageFns<SetSettings> = {
  encode(message: SetSettings, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.domain !== "") {
      writer.uint32(10).string(message.domain);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetSettings {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetSettings();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.domain = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SetSettings {
    return { domain: isSet(object.domain) ? globalThis.String(object.domain) : "" };
  },

  toJSON(message: SetSettings): unknown {
    const obj: any = {};
    if (message.domain !== "") {
      obj.domain = message.domain;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SetSettings>, I>>(base?: I): SetSettings {
    return SetSettings.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SetSettings>, I>>(object: I): SetSettings {
    const message = createBaseSetSettings();
    message.domain = object.domain ?? "";
    return message;
  },
};

function createBaseSettings(): Settings {
  return { domain: "", bridgeSecret: "" };
}

export const Settings: MessageFns<Settings> = {
  encode(message: Settings, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.domain !== "") {
      writer.uint32(10).string(message.domain);
    }
    if (message.bridgeSecret !== "") {
      writer.uint32(18).string(message.bridgeSecret);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Settings {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSettings();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.domain = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.bridgeSecret = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Settings {
    return {
      domain: isSet(object.domain) ? globalThis.String(object.domain) : "",
      bridgeSecret: isSet(object.bridgeSecret) ? globalThis.String(object.bridgeSecret) : "",
    };
  },

  toJSON(message: Settings): unknown {
    const obj: any = {};
    if (message.domain !== "") {
      obj.domain = message.domain;
    }
    if (message.bridgeSecret !== "") {
      obj.bridgeSecret = message.bridgeSecret;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Settings>, I>>(base?: I): Settings {
    return Settings.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Settings>, I>>(object: I): Settings {
    const message = createBaseSettings();
    message.domain = object.domain ?? "";
    message.bridgeSecret = object.bridgeSecret ?? "";
    return message;
  },
};

function createBaseSetBridgeSecret(): SetBridgeSecret {
  return { secret: "" };
}

export const SetBridgeSecret: MessageFns<SetBridgeSecret> = {
  encode(message: SetBridgeSecret, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.secret !== "") {
      writer.uint32(10).string(message.secret);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetBridgeSecret {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetBridgeSecret();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.secret = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SetBridgeSecret {
    return { secret: isSet(object.secret) ? globalThis.String(object.secret) : "" };
  },

  toJSON(message: SetBridgeSecret): unknown {
    const obj: any = {};
    if (message.secret !== "") {
      obj.secret = message.secret;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SetBridgeSecret>, I>>(base?: I): SetBridgeSecret {
    return SetBridgeSecret.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SetBridgeSecret>, I>>(object: I): SetBridgeSecret {
    const message = createBaseSetBridgeSecret();
    message.secret = object.secret ?? "";
    return message;
  },
};

function createBaseListStorageDevices(): ListStorageDevices {
  return {};
}

export const ListStorageDevices: MessageFns<ListStorageDevices> = {
  encode(_: ListStorageDevices, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListStorageDevices {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListStorageDevices();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): ListStorageDevices {
    return {};
  },

  toJSON(_: ListStorageDevices): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<ListStorageDevices>, I>>(base?: I): ListStorageDevices {
    return ListStorageDevices.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListStorageDevices>, I>>(_: I): ListStorageDevices {
    const message = createBaseListStorageDevices();
    return message;
  },
};

function createBaseStorageDevice(): StorageDevice {
  return { path: "", sizeBytes: 0n, model: "" };
}

export const StorageDevice: MessageFns<StorageDevice> = {
  encode(message: StorageDevice, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.path !== "") {
      writer.uint32(10).string(message.path);
    }
    if (message.sizeBytes !== 0n) {
      if (BigInt.asIntN(64, message.sizeBytes) !== message.sizeBytes) {
        throw new globalThis.Error("value provided for field message.sizeBytes of type int64 too large");
      }
      writer.uint32(16).int64(message.sizeBytes);
    }
    if (message.model !== "") {
      writer.uint32(26).string(message.model);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StorageDevice {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStorageDevice();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.path = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.sizeBytes = reader.int64() as bigint;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.model = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StorageDevice {
    return {
      path: isSet(object.path) ? globalThis.String(object.path) : "",
      sizeBytes: isSet(object.sizeBytes) ? BigInt(object.sizeBytes) : 0n,
      model: isSet(object.model) ? globalThis.String(object.model) : "",
    };
  },

  toJSON(message: StorageDevice): unknown {
    const obj: any = {};
    if (message.path !== "") {
      obj.path = message.path;
    }
    if (message.sizeBytes !== 0n) {
      obj.sizeBytes = message.sizeBytes.toString();
    }
    if (message.model !== "") {
      obj.model = message.model;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StorageDevice>, I>>(base?: I): StorageDevice {
    return StorageDevice.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StorageDevice>, I>>(object: I): StorageDevice {
    const message = createBaseStorageDevice();
    message.path = object.path ?? "";
    message.sizeBytes = object.sizeBytes ?? 0n;
    message.model = object.model ?? "";
    return message;
  },
};

function createBaseStorageDevices(): StorageDevices {
  return { devices: [] };
}

export const StorageDevices: MessageFns<StorageDevices> = {
  encode(message: StorageDevices, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.devices) {
      StorageDevice.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StorageDevices {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStorageDevices();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.devices.push(StorageDevice.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StorageDevices {
    return {
      devices: globalThis.Array.isArray(object?.devices)
        ? object.devices.map((e: any) => StorageDevice.fromJSON(e))
        : [],
    };
  },

  toJSON(message: StorageDevices): unknown {
    const obj: any = {};
    if (message.devices?.length) {
      obj.devices = message.devices.map((e) => StorageDevice.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StorageDevices>, I>>(base?: I): StorageDevices {
    return StorageDevices.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StorageDevices>, I>>(object: I): StorageDevices {
    const message = createBaseStorageDevices();
    message.devices = object.devices?.map((e) => StorageDevice.fromPartial(e)) || [];
    return message;
  },
};

function createBaseSetupStorage(): SetupStorage {
  return { devicePaths: [] };
}

export const SetupStorage: MessageFns<SetupStorage> = {
  encode(message: SetupStorage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.devicePaths) {
      writer.uint32(10).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetupStorage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetupStorage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.devicePaths.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SetupStorage {
    return {
      devicePaths: globalThis.Array.isArray(object?.devicePaths)
        ? object.devicePaths.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: SetupStorage): unknown {
    const obj: any = {};
    if (message.devicePaths?.length) {
      obj.devicePaths = message.devicePaths;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SetupStorage>, I>>(base?: I): SetupStorage {
    return SetupStorage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SetupStorage>, I>>(object: I): SetupStorage {
    const message = createBaseSetupStorage();
    message.devicePaths = object.devicePaths?.map((e) => e) || [];
    return message;
  },
};

function createBaseListWifiNetworks(): ListWifiNetworks {
  return {};
}

export const ListWifiNetworks: MessageFns<ListWifiNetworks> = {
  encode(_: ListWifiNetworks, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListWifiNetworks {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListWifiNetworks();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): ListWifiNetworks {
    return {};
  },

  toJSON(_: ListWifiNetworks): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<ListWifiNetworks>, I>>(base?: I): ListWifiNetworks {
    return ListWifiNetworks.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListWifiNetworks>, I>>(_: I): ListWifiNetworks {
    const message = createBaseListWifiNetworks();
    return message;
  },
};

function createBaseWifiNetwork(): WifiNetwork {
  return { ssid: "", signal: 0, secured: false };
}

export const WifiNetwork: MessageFns<WifiNetwork> = {
  encode(message: WifiNetwork, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ssid !== "") {
      writer.uint32(10).string(message.ssid);
    }
    if (message.signal !== 0) {
      writer.uint32(16).int32(message.signal);
    }
    if (message.secured !== false) {
      writer.uint32(24).bool(message.secured);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WifiNetwork {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWifiNetwork();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.ssid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.signal = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.secured = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WifiNetwork {
    return {
      ssid: isSet(object.ssid) ? globalThis.String(object.ssid) : "",
      signal: isSet(object.signal) ? globalThis.Number(object.signal) : 0,
      secured: isSet(object.secured) ? globalThis.Boolean(object.secured) : false,
    };
  },

  toJSON(message: WifiNetwork): unknown {
    const obj: any = {};
    if (message.ssid !== "") {
      obj.ssid = message.ssid;
    }
    if (message.signal !== 0) {
      obj.signal = Math.round(message.signal);
    }
    if (message.secured !== false) {
      obj.secured = message.secured;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WifiNetwork>, I>>(base?: I): WifiNetwork {
    return WifiNetwork.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WifiNetwork>, I>>(object: I): WifiNetwork {
    const message = createBaseWifiNetwork();
    message.ssid = object.ssid ?? "";
    message.signal = object.signal ?? 0;
    message.secured = object.secured ?? false;
    return message;
  },
};

function createBaseWifiNetworks(): WifiNetworks {
  return { networks: [] };
}

export const WifiNetworks: MessageFns<WifiNetworks> = {
  encode(message: WifiNetworks, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.networks) {
      WifiNetwork.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WifiNetworks {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWifiNetworks();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.networks.push(WifiNetwork.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WifiNetworks {
    return {
      networks: globalThis.Array.isArray(object?.networks)
        ? object.networks.map((e: any) => WifiNetwork.fromJSON(e))
        : [],
    };
  },

  toJSON(message: WifiNetworks): unknown {
    const obj: any = {};
    if (message.networks?.length) {
      obj.networks = message.networks.map((e) => WifiNetwork.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WifiNetworks>, I>>(base?: I): WifiNetworks {
    return WifiNetworks.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WifiNetworks>, I>>(object: I): WifiNetworks {
    const message = createBaseWifiNetworks();
    message.networks = object.networks?.map((e) => WifiNetwork.fromPartial(e)) || [];
    return message;
  },
};

function createBaseSetWifi(): SetWifi {
  return { ssid: "", password: "" };
}

export const SetWifi: MessageFns<SetWifi> = {
  encode(message: SetWifi, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ssid !== "") {
      writer.uint32(10).string(message.ssid);
    }
    if (message.password !== "") {
      writer.uint32(18).string(message.password);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetWifi {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetWifi();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.ssid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.password = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SetWifi {
    return {
      ssid: isSet(object.ssid) ? globalThis.String(object.ssid) : "",
      password: isSet(object.password) ? globalThis.String(object.password) : "",
    };
  },

  toJSON(message: SetWifi): unknown {
    const obj: any = {};
    if (message.ssid !== "") {
      obj.ssid = message.ssid;
    }
    if (message.password !== "") {
      obj.password = message.password;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SetWifi>, I>>(base?: I): SetWifi {
    return SetWifi.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SetWifi>, I>>(object: I): SetWifi {
    const message = createBaseSetWifi();
    message.ssid = object.ssid ?? "";
    message.password = object.password ?? "";
    return message;
  },
};

function createBaseBridgeRegister(): BridgeRegister {
  return { ownerUuid: "", domain: "", secret: "" };
}

export const BridgeRegister: MessageFns<BridgeRegister> = {
  encode(message: BridgeRegister, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ownerUuid !== "") {
      writer.uint32(10).string(message.ownerUuid);
    }
    if (message.domain !== "") {
      writer.uint32(18).string(message.domain);
    }
    if (message.secret !== "") {
      writer.uint32(26).string(message.secret);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BridgeRegister {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBridgeRegister();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.ownerUuid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.domain = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.secret = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BridgeRegister {
    return {
      ownerUuid: isSet(object.ownerUuid) ? globalThis.String(object.ownerUuid) : "",
      domain: isSet(object.domain) ? globalThis.String(object.domain) : "",
      secret: isSet(object.secret) ? globalThis.String(object.secret) : "",
    };
  },

  toJSON(message: BridgeRegister): unknown {
    const obj: any = {};
    if (message.ownerUuid !== "") {
      obj.ownerUuid = message.ownerUuid;
    }
    if (message.domain !== "") {
      obj.domain = message.domain;
    }
    if (message.secret !== "") {
      obj.secret = message.secret;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BridgeRegister>, I>>(base?: I): BridgeRegister {
    return BridgeRegister.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BridgeRegister>, I>>(object: I): BridgeRegister {
    const message = createBaseBridgeRegister();
    message.ownerUuid = object.ownerUuid ?? "";
    message.domain = object.domain ?? "";
    message.secret = object.secret ?? "";
    return message;
  },
};

function createBaseBridgeAckOnboard(): BridgeAckOnboard {
  return { ok: false };
}

export const BridgeAckOnboard: MessageFns<BridgeAckOnboard> = {
  encode(message: BridgeAckOnboard, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ok !== false) {
      writer.uint32(8).bool(message.ok);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BridgeAckOnboard {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBridgeAckOnboard();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.ok = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BridgeAckOnboard {
    return { ok: isSet(object.ok) ? globalThis.Boolean(object.ok) : false };
  },

  toJSON(message: BridgeAckOnboard): unknown {
    const obj: any = {};
    if (message.ok !== false) {
      obj.ok = message.ok;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BridgeAckOnboard>, I>>(base?: I): BridgeAckOnboard {
    return BridgeAckOnboard.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BridgeAckOnboard>, I>>(object: I): BridgeAckOnboard {
    const message = createBaseBridgeAckOnboard();
    message.ok = object.ok ?? false;
    return message;
  },
};

function createBaseRotateBridgeSecret(): RotateBridgeSecret {
  return { ownerUuid: "", domain: "", secret: "" };
}

export const RotateBridgeSecret: MessageFns<RotateBridgeSecret> = {
  encode(message: RotateBridgeSecret, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ownerUuid !== "") {
      writer.uint32(10).string(message.ownerUuid);
    }
    if (message.domain !== "") {
      writer.uint32(18).string(message.domain);
    }
    if (message.secret !== "") {
      writer.uint32(26).string(message.secret);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RotateBridgeSecret {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRotateBridgeSecret();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.ownerUuid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.domain = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.secret = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RotateBridgeSecret {
    return {
      ownerUuid: isSet(object.ownerUuid) ? globalThis.String(object.ownerUuid) : "",
      domain: isSet(object.domain) ? globalThis.String(object.domain) : "",
      secret: isSet(object.secret) ? globalThis.String(object.secret) : "",
    };
  },

  toJSON(message: RotateBridgeSecret): unknown {
    const obj: any = {};
    if (message.ownerUuid !== "") {
      obj.ownerUuid = message.ownerUuid;
    }
    if (message.domain !== "") {
      obj.domain = message.domain;
    }
    if (message.secret !== "") {
      obj.secret = message.secret;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RotateBridgeSecret>, I>>(base?: I): RotateBridgeSecret {
    return RotateBridgeSecret.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RotateBridgeSecret>, I>>(object: I): RotateBridgeSecret {
    const message = createBaseRotateBridgeSecret();
    message.ownerUuid = object.ownerUuid ?? "";
    message.domain = object.domain ?? "";
    message.secret = object.secret ?? "";
    return message;
  },
};

function createBaseRotateBridgeSecretAck(): RotateBridgeSecretAck {
  return { newSecret: "" };
}

export const RotateBridgeSecretAck: MessageFns<RotateBridgeSecretAck> = {
  encode(message: RotateBridgeSecretAck, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.newSecret !== "") {
      writer.uint32(10).string(message.newSecret);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RotateBridgeSecretAck {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRotateBridgeSecretAck();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.newSecret = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RotateBridgeSecretAck {
    return { newSecret: isSet(object.newSecret) ? globalThis.String(object.newSecret) : "" };
  },

  toJSON(message: RotateBridgeSecretAck): unknown {
    const obj: any = {};
    if (message.newSecret !== "") {
      obj.newSecret = message.newSecret;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RotateBridgeSecretAck>, I>>(base?: I): RotateBridgeSecretAck {
    return RotateBridgeSecretAck.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RotateBridgeSecretAck>, I>>(object: I): RotateBridgeSecretAck {
    const message = createBaseRotateBridgeSecretAck();
    message.newSecret = object.newSecret ?? "";
    return message;
  },
};

function createBaseRegenerateBridgeSecret(): RegenerateBridgeSecret {
  return {};
}

export const RegenerateBridgeSecret: MessageFns<RegenerateBridgeSecret> = {
  encode(_: RegenerateBridgeSecret, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RegenerateBridgeSecret {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegenerateBridgeSecret();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): RegenerateBridgeSecret {
    return {};
  },

  toJSON(_: RegenerateBridgeSecret): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<RegenerateBridgeSecret>, I>>(base?: I): RegenerateBridgeSecret {
    return RegenerateBridgeSecret.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RegenerateBridgeSecret>, I>>(_: I): RegenerateBridgeSecret {
    const message = createBaseRegenerateBridgeSecret();
    return message;
  },
};

function createBaseGetProfile(): GetProfile {
  return {};
}

export const GetProfile: MessageFns<GetProfile> = {
  encode(_: GetProfile, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetProfile {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetProfile();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): GetProfile {
    return {};
  },

  toJSON(_: GetProfile): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<GetProfile>, I>>(base?: I): GetProfile {
    return GetProfile.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetProfile>, I>>(_: I): GetProfile {
    const message = createBaseGetProfile();
    return message;
  },
};

function createBaseProfile(): Profile {
  return { name: "", image: undefined, text: "", domain: "" };
}

export const Profile: MessageFns<Profile> = {
  encode(message: Profile, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.image !== undefined) {
      writer.uint32(18).bytes(message.image);
    }
    if (message.text !== "") {
      writer.uint32(26).string(message.text);
    }
    if (message.domain !== "") {
      writer.uint32(34).string(message.domain);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Profile {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProfile();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.image = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.text = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.domain = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Profile {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      image: isSet(object.image) ? bytesFromBase64(object.image) : undefined,
      text: isSet(object.text) ? globalThis.String(object.text) : "",
      domain: isSet(object.domain) ? globalThis.String(object.domain) : "",
    };
  },

  toJSON(message: Profile): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.image !== undefined) {
      obj.image = base64FromBytes(message.image);
    }
    if (message.text !== "") {
      obj.text = message.text;
    }
    if (message.domain !== "") {
      obj.domain = message.domain;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Profile>, I>>(base?: I): Profile {
    return Profile.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Profile>, I>>(object: I): Profile {
    const message = createBaseProfile();
    message.name = object.name ?? "";
    message.image = object.image ?? undefined;
    message.text = object.text ?? "";
    message.domain = object.domain ?? "";
    return message;
  },
};

function createBaseShareFilesLink(): ShareFilesLink {
  return { paths: [] };
}

export const ShareFilesLink: MessageFns<ShareFilesLink> = {
  encode(message: ShareFilesLink, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.paths) {
      writer.uint32(10).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ShareFilesLink {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseShareFilesLink();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.paths.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ShareFilesLink {
    return { paths: globalThis.Array.isArray(object?.paths) ? object.paths.map((e: any) => globalThis.String(e)) : [] };
  },

  toJSON(message: ShareFilesLink): unknown {
    const obj: any = {};
    if (message.paths?.length) {
      obj.paths = message.paths;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ShareFilesLink>, I>>(base?: I): ShareFilesLink {
    return ShareFilesLink.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ShareFilesLink>, I>>(object: I): ShareFilesLink {
    const message = createBaseShareFilesLink();
    message.paths = object.paths?.map((e) => e) || [];
    return message;
  },
};

function createBaseShareLink(): ShareLink {
  return { link: "" };
}

export const ShareLink: MessageFns<ShareLink> = {
  encode(message: ShareLink, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.link !== "") {
      writer.uint32(10).string(message.link);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ShareLink {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseShareLink();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.link = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ShareLink {
    return { link: isSet(object.link) ? globalThis.String(object.link) : "" };
  },

  toJSON(message: ShareLink): unknown {
    const obj: any = {};
    if (message.link !== "") {
      obj.link = message.link;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ShareLink>, I>>(base?: I): ShareLink {
    return ShareLink.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ShareLink>, I>>(object: I): ShareLink {
    const message = createBaseShareLink();
    message.link = object.link ?? "";
    return message;
  },
};

function createBaseDownloadSharedLink(): DownloadSharedLink {
  return { uuid: "", secret: "" };
}

export const DownloadSharedLink: MessageFns<DownloadSharedLink> = {
  encode(message: DownloadSharedLink, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.uuid !== "") {
      writer.uint32(10).string(message.uuid);
    }
    if (message.secret !== "") {
      writer.uint32(18).string(message.secret);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DownloadSharedLink {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDownloadSharedLink();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.uuid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.secret = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DownloadSharedLink {
    return {
      uuid: isSet(object.uuid) ? globalThis.String(object.uuid) : "",
      secret: isSet(object.secret) ? globalThis.String(object.secret) : "",
    };
  },

  toJSON(message: DownloadSharedLink): unknown {
    const obj: any = {};
    if (message.uuid !== "") {
      obj.uuid = message.uuid;
    }
    if (message.secret !== "") {
      obj.secret = message.secret;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DownloadSharedLink>, I>>(base?: I): DownloadSharedLink {
    return DownloadSharedLink.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DownloadSharedLink>, I>>(object: I): DownloadSharedLink {
    const message = createBaseDownloadSharedLink();
    message.uuid = object.uuid ?? "";
    message.secret = object.secret ?? "";
    return message;
  },
};

function createBaseNewSocial(): NewSocial {
  return { uuid: "" };
}

export const NewSocial: MessageFns<NewSocial> = {
  encode(message: NewSocial, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.uuid !== "") {
      writer.uint32(10).string(message.uuid);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NewSocial {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNewSocial();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.uuid = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): NewSocial {
    return { uuid: isSet(object.uuid) ? globalThis.String(object.uuid) : "" };
  },

  toJSON(message: NewSocial): unknown {
    const obj: any = {};
    if (message.uuid !== "") {
      obj.uuid = message.uuid;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<NewSocial>, I>>(base?: I): NewSocial {
    return NewSocial.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<NewSocial>, I>>(object: I): NewSocial {
    const message = createBaseNewSocial();
    message.uuid = object.uuid ?? "";
    return message;
  },
};

function createBaseSharedFiles(): SharedFiles {
  return { content: new Uint8Array(0) };
}

export const SharedFiles: MessageFns<SharedFiles> = {
  encode(message: SharedFiles, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.content.length !== 0) {
      writer.uint32(10).bytes(message.content);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SharedFiles {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSharedFiles();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.content = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SharedFiles {
    return { content: isSet(object.content) ? bytesFromBase64(object.content) : new Uint8Array(0) };
  },

  toJSON(message: SharedFiles): unknown {
    const obj: any = {};
    if (message.content.length !== 0) {
      obj.content = base64FromBytes(message.content);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SharedFiles>, I>>(base?: I): SharedFiles {
    return SharedFiles.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SharedFiles>, I>>(object: I): SharedFiles {
    const message = createBaseSharedFiles();
    message.content = object.content ?? new Uint8Array(0);
    return message;
  },
};

function createBaseComment(): Comment {
  return { pubUuid: "", commentUuid: "", comment: "", publisher: "", likes: 0, dateTime: undefined, liked: false };
}

export const Comment: MessageFns<Comment> = {
  encode(message: Comment, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.pubUuid !== "") {
      writer.uint32(10).string(message.pubUuid);
    }
    if (message.commentUuid !== "") {
      writer.uint32(18).string(message.commentUuid);
    }
    if (message.comment !== "") {
      writer.uint32(26).string(message.comment);
    }
    if (message.publisher !== "") {
      writer.uint32(34).string(message.publisher);
    }
    if (message.likes !== 0) {
      writer.uint32(40).int32(message.likes);
    }
    if (message.dateTime !== undefined) {
      Timestamp.encode(toTimestamp(message.dateTime), writer.uint32(50).fork()).join();
    }
    if (message.liked !== false) {
      writer.uint32(56).bool(message.liked);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Comment {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseComment();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.pubUuid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.commentUuid = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.comment = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.publisher = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.likes = reader.int32();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.dateTime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.liked = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Comment {
    return {
      pubUuid: isSet(object.pubUuid) ? globalThis.String(object.pubUuid) : "",
      commentUuid: isSet(object.commentUuid) ? globalThis.String(object.commentUuid) : "",
      comment: isSet(object.comment) ? globalThis.String(object.comment) : "",
      publisher: isSet(object.publisher) ? globalThis.String(object.publisher) : "",
      likes: isSet(object.likes) ? globalThis.Number(object.likes) : 0,
      dateTime: isSet(object.dateTime) ? fromJsonTimestamp(object.dateTime) : undefined,
      liked: isSet(object.liked) ? globalThis.Boolean(object.liked) : false,
    };
  },

  toJSON(message: Comment): unknown {
    const obj: any = {};
    if (message.pubUuid !== "") {
      obj.pubUuid = message.pubUuid;
    }
    if (message.commentUuid !== "") {
      obj.commentUuid = message.commentUuid;
    }
    if (message.comment !== "") {
      obj.comment = message.comment;
    }
    if (message.publisher !== "") {
      obj.publisher = message.publisher;
    }
    if (message.likes !== 0) {
      obj.likes = Math.round(message.likes);
    }
    if (message.dateTime !== undefined) {
      obj.dateTime = message.dateTime.toISOString();
    }
    if (message.liked !== false) {
      obj.liked = message.liked;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Comment>, I>>(base?: I): Comment {
    return Comment.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Comment>, I>>(object: I): Comment {
    const message = createBaseComment();
    message.pubUuid = object.pubUuid ?? "";
    message.commentUuid = object.commentUuid ?? "";
    message.comment = object.comment ?? "";
    message.publisher = object.publisher ?? "";
    message.likes = object.likes ?? 0;
    message.dateTime = object.dateTime ?? undefined;
    message.liked = object.liked ?? false;
    return message;
  },
};

function createBaseSocialPublication(): SocialPublication {
  return {
    uuid: "",
    text: "",
    files: [],
    comments: [],
    likes: 0,
    publisher: undefined,
    liked: false,
    own: false,
    dateTime: undefined,
  };
}

export const SocialPublication: MessageFns<SocialPublication> = {
  encode(message: SocialPublication, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.uuid !== "") {
      writer.uint32(10).string(message.uuid);
    }
    if (message.text !== "") {
      writer.uint32(18).string(message.text);
    }
    for (const v of message.files) {
      File.encode(v!, writer.uint32(26).fork()).join();
    }
    for (const v of message.comments) {
      Comment.encode(v!, writer.uint32(34).fork()).join();
    }
    if (message.likes !== 0) {
      writer.uint32(40).int32(message.likes);
    }
    if (message.publisher !== undefined) {
      Profile.encode(message.publisher, writer.uint32(58).fork()).join();
    }
    if (message.liked !== false) {
      writer.uint32(64).bool(message.liked);
    }
    if (message.own !== false) {
      writer.uint32(72).bool(message.own);
    }
    if (message.dateTime !== undefined) {
      Timestamp.encode(toTimestamp(message.dateTime), writer.uint32(82).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SocialPublication {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSocialPublication();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.uuid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.text = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.files.push(File.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.comments.push(Comment.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.likes = reader.int32();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.publisher = Profile.decode(reader, reader.uint32());
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.liked = reader.bool();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.own = reader.bool();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.dateTime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SocialPublication {
    return {
      uuid: isSet(object.uuid) ? globalThis.String(object.uuid) : "",
      text: isSet(object.text) ? globalThis.String(object.text) : "",
      files: globalThis.Array.isArray(object?.files) ? object.files.map((e: any) => File.fromJSON(e)) : [],
      comments: globalThis.Array.isArray(object?.comments) ? object.comments.map((e: any) => Comment.fromJSON(e)) : [],
      likes: isSet(object.likes) ? globalThis.Number(object.likes) : 0,
      publisher: isSet(object.publisher) ? Profile.fromJSON(object.publisher) : undefined,
      liked: isSet(object.liked) ? globalThis.Boolean(object.liked) : false,
      own: isSet(object.own) ? globalThis.Boolean(object.own) : false,
      dateTime: isSet(object.dateTime) ? fromJsonTimestamp(object.dateTime) : undefined,
    };
  },

  toJSON(message: SocialPublication): unknown {
    const obj: any = {};
    if (message.uuid !== "") {
      obj.uuid = message.uuid;
    }
    if (message.text !== "") {
      obj.text = message.text;
    }
    if (message.files?.length) {
      obj.files = message.files.map((e) => File.toJSON(e));
    }
    if (message.comments?.length) {
      obj.comments = message.comments.map((e) => Comment.toJSON(e));
    }
    if (message.likes !== 0) {
      obj.likes = Math.round(message.likes);
    }
    if (message.publisher !== undefined) {
      obj.publisher = Profile.toJSON(message.publisher);
    }
    if (message.liked !== false) {
      obj.liked = message.liked;
    }
    if (message.own !== false) {
      obj.own = message.own;
    }
    if (message.dateTime !== undefined) {
      obj.dateTime = message.dateTime.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SocialPublication>, I>>(base?: I): SocialPublication {
    return SocialPublication.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SocialPublication>, I>>(object: I): SocialPublication {
    const message = createBaseSocialPublication();
    message.uuid = object.uuid ?? "";
    message.text = object.text ?? "";
    message.files = object.files?.map((e) => File.fromPartial(e)) || [];
    message.comments = object.comments?.map((e) => Comment.fromPartial(e)) || [];
    message.likes = object.likes ?? 0;
    message.publisher = (object.publisher !== undefined && object.publisher !== null)
      ? Profile.fromPartial(object.publisher)
      : undefined;
    message.liked = object.liked ?? false;
    message.own = object.own ?? false;
    message.dateTime = object.dateTime ?? undefined;
    return message;
  },
};

function createBaseSocialPublicationFiles(): SocialPublicationFiles {
  return { files: [] };
}

export const SocialPublicationFiles: MessageFns<SocialPublicationFiles> = {
  encode(message: SocialPublicationFiles, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.files) {
      File.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SocialPublicationFiles {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSocialPublicationFiles();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.files.push(File.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SocialPublicationFiles {
    return { files: globalThis.Array.isArray(object?.files) ? object.files.map((e: any) => File.fromJSON(e)) : [] };
  },

  toJSON(message: SocialPublicationFiles): unknown {
    const obj: any = {};
    if (message.files?.length) {
      obj.files = message.files.map((e) => File.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SocialPublicationFiles>, I>>(base?: I): SocialPublicationFiles {
    return SocialPublicationFiles.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SocialPublicationFiles>, I>>(object: I): SocialPublicationFiles {
    const message = createBaseSocialPublicationFiles();
    message.files = object.files?.map((e) => File.fromPartial(e)) || [];
    return message;
  },
};

function createBaseFileExifInfo(): FileExifInfo {
  return {
    cameraMake: "",
    cameraModel: "",
    takenAt: undefined,
    exposureTime: "",
    fNumber: "",
    iso: 0,
    focalLength: "",
    width: 0,
    height: 0,
    hasGps: false,
    latitude: 0,
    longitude: 0,
    city: "",
    country: "",
  };
}

export const FileExifInfo: MessageFns<FileExifInfo> = {
  encode(message: FileExifInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.cameraMake !== "") {
      writer.uint32(10).string(message.cameraMake);
    }
    if (message.cameraModel !== "") {
      writer.uint32(18).string(message.cameraModel);
    }
    if (message.takenAt !== undefined) {
      Timestamp.encode(toTimestamp(message.takenAt), writer.uint32(26).fork()).join();
    }
    if (message.exposureTime !== "") {
      writer.uint32(34).string(message.exposureTime);
    }
    if (message.fNumber !== "") {
      writer.uint32(42).string(message.fNumber);
    }
    if (message.iso !== 0) {
      writer.uint32(48).int32(message.iso);
    }
    if (message.focalLength !== "") {
      writer.uint32(58).string(message.focalLength);
    }
    if (message.width !== 0) {
      writer.uint32(64).int32(message.width);
    }
    if (message.height !== 0) {
      writer.uint32(72).int32(message.height);
    }
    if (message.hasGps !== false) {
      writer.uint32(80).bool(message.hasGps);
    }
    if (message.latitude !== 0) {
      writer.uint32(89).double(message.latitude);
    }
    if (message.longitude !== 0) {
      writer.uint32(97).double(message.longitude);
    }
    if (message.city !== "") {
      writer.uint32(106).string(message.city);
    }
    if (message.country !== "") {
      writer.uint32(114).string(message.country);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FileExifInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFileExifInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.cameraMake = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.cameraModel = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.takenAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.exposureTime = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.fNumber = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.iso = reader.int32();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.focalLength = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.width = reader.int32();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.height = reader.int32();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.hasGps = reader.bool();
          continue;
        }
        case 11: {
          if (tag !== 89) {
            break;
          }

          message.latitude = reader.double();
          continue;
        }
        case 12: {
          if (tag !== 97) {
            break;
          }

          message.longitude = reader.double();
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.city = reader.string();
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.country = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FileExifInfo {
    return {
      cameraMake: isSet(object.cameraMake) ? globalThis.String(object.cameraMake) : "",
      cameraModel: isSet(object.cameraModel) ? globalThis.String(object.cameraModel) : "",
      takenAt: isSet(object.takenAt) ? fromJsonTimestamp(object.takenAt) : undefined,
      exposureTime: isSet(object.exposureTime) ? globalThis.String(object.exposureTime) : "",
      fNumber: isSet(object.fNumber) ? globalThis.String(object.fNumber) : "",
      iso: isSet(object.iso) ? globalThis.Number(object.iso) : 0,
      focalLength: isSet(object.focalLength) ? globalThis.String(object.focalLength) : "",
      width: isSet(object.width) ? globalThis.Number(object.width) : 0,
      height: isSet(object.height) ? globalThis.Number(object.height) : 0,
      hasGps: isSet(object.hasGps) ? globalThis.Boolean(object.hasGps) : false,
      latitude: isSet(object.latitude) ? globalThis.Number(object.latitude) : 0,
      longitude: isSet(object.longitude) ? globalThis.Number(object.longitude) : 0,
      city: isSet(object.city) ? globalThis.String(object.city) : "",
      country: isSet(object.country) ? globalThis.String(object.country) : "",
    };
  },

  toJSON(message: FileExifInfo): unknown {
    const obj: any = {};
    if (message.cameraMake !== "") {
      obj.cameraMake = message.cameraMake;
    }
    if (message.cameraModel !== "") {
      obj.cameraModel = message.cameraModel;
    }
    if (message.takenAt !== undefined) {
      obj.takenAt = message.takenAt.toISOString();
    }
    if (message.exposureTime !== "") {
      obj.exposureTime = message.exposureTime;
    }
    if (message.fNumber !== "") {
      obj.fNumber = message.fNumber;
    }
    if (message.iso !== 0) {
      obj.iso = Math.round(message.iso);
    }
    if (message.focalLength !== "") {
      obj.focalLength = message.focalLength;
    }
    if (message.width !== 0) {
      obj.width = Math.round(message.width);
    }
    if (message.height !== 0) {
      obj.height = Math.round(message.height);
    }
    if (message.hasGps !== false) {
      obj.hasGps = message.hasGps;
    }
    if (message.latitude !== 0) {
      obj.latitude = message.latitude;
    }
    if (message.longitude !== 0) {
      obj.longitude = message.longitude;
    }
    if (message.city !== "") {
      obj.city = message.city;
    }
    if (message.country !== "") {
      obj.country = message.country;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FileExifInfo>, I>>(base?: I): FileExifInfo {
    return FileExifInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FileExifInfo>, I>>(object: I): FileExifInfo {
    const message = createBaseFileExifInfo();
    message.cameraMake = object.cameraMake ?? "";
    message.cameraModel = object.cameraModel ?? "";
    message.takenAt = object.takenAt ?? undefined;
    message.exposureTime = object.exposureTime ?? "";
    message.fNumber = object.fNumber ?? "";
    message.iso = object.iso ?? 0;
    message.focalLength = object.focalLength ?? "";
    message.width = object.width ?? 0;
    message.height = object.height ?? 0;
    message.hasGps = object.hasGps ?? false;
    message.latitude = object.latitude ?? 0;
    message.longitude = object.longitude ?? 0;
    message.city = object.city ?? "";
    message.country = object.country ?? "";
    return message;
  },
};

function createBaseGetFileInfo(): GetFileInfo {
  return { path: "" };
}

export const GetFileInfo: MessageFns<GetFileInfo> = {
  encode(message: GetFileInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.path !== "") {
      writer.uint32(10).string(message.path);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetFileInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetFileInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.path = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetFileInfo {
    return { path: isSet(object.path) ? globalThis.String(object.path) : "" };
  },

  toJSON(message: GetFileInfo): unknown {
    const obj: any = {};
    if (message.path !== "") {
      obj.path = message.path;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetFileInfo>, I>>(base?: I): GetFileInfo {
    return GetFileInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetFileInfo>, I>>(object: I): GetFileInfo {
    const message = createBaseGetFileInfo();
    message.path = object.path ?? "";
    return message;
  },
};

function createBaseSocialPublications(): SocialPublications {
  return { publications: [], since: undefined };
}

export const SocialPublications: MessageFns<SocialPublications> = {
  encode(message: SocialPublications, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.publications) {
      SocialPublication.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.since !== undefined) {
      Timestamp.encode(toTimestamp(message.since), writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SocialPublications {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSocialPublications();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.publications.push(SocialPublication.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.since = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SocialPublications {
    return {
      publications: globalThis.Array.isArray(object?.publications)
        ? object.publications.map((e: any) => SocialPublication.fromJSON(e))
        : [],
      since: isSet(object.since) ? fromJsonTimestamp(object.since) : undefined,
    };
  },

  toJSON(message: SocialPublications): unknown {
    const obj: any = {};
    if (message.publications?.length) {
      obj.publications = message.publications.map((e) => SocialPublication.toJSON(e));
    }
    if (message.since !== undefined) {
      obj.since = message.since.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SocialPublications>, I>>(base?: I): SocialPublications {
    return SocialPublications.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SocialPublications>, I>>(object: I): SocialPublications {
    const message = createBaseSocialPublications();
    message.publications = object.publications?.map((e) => SocialPublication.fromPartial(e)) || [];
    message.since = object.since ?? undefined;
    return message;
  },
};

function createBaseEvent(): Event {
  return { uuid: "", dt: undefined, type: "", content: "" };
}

export const Event: MessageFns<Event> = {
  encode(message: Event, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.uuid !== "") {
      writer.uint32(10).string(message.uuid);
    }
    if (message.dt !== undefined) {
      Timestamp.encode(toTimestamp(message.dt), writer.uint32(18).fork()).join();
    }
    if (message.type !== "") {
      writer.uint32(26).string(message.type);
    }
    if (message.content !== "") {
      writer.uint32(34).string(message.content);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Event {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.uuid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.dt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.type = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.content = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Event {
    return {
      uuid: isSet(object.uuid) ? globalThis.String(object.uuid) : "",
      dt: isSet(object.dt) ? fromJsonTimestamp(object.dt) : undefined,
      type: isSet(object.type) ? globalThis.String(object.type) : "",
      content: isSet(object.content) ? globalThis.String(object.content) : "",
    };
  },

  toJSON(message: Event): unknown {
    const obj: any = {};
    if (message.uuid !== "") {
      obj.uuid = message.uuid;
    }
    if (message.dt !== undefined) {
      obj.dt = message.dt.toISOString();
    }
    if (message.type !== "") {
      obj.type = message.type;
    }
    if (message.content !== "") {
      obj.content = message.content;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Event>, I>>(base?: I): Event {
    return Event.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Event>, I>>(object: I): Event {
    const message = createBaseEvent();
    message.uuid = object.uuid ?? "";
    message.dt = object.dt ?? undefined;
    message.type = object.type ?? "";
    message.content = object.content ?? "";
    return message;
  },
};

function createBaseEvents(): Events {
  return { events: [], since: undefined };
}

export const Events: MessageFns<Events> = {
  encode(message: Events, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.events) {
      Event.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.since !== undefined) {
      Timestamp.encode(toTimestamp(message.since), writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Events {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEvents();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.events.push(Event.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.since = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Events {
    return {
      events: globalThis.Array.isArray(object?.events) ? object.events.map((e: any) => Event.fromJSON(e)) : [],
      since: isSet(object.since) ? fromJsonTimestamp(object.since) : undefined,
    };
  },

  toJSON(message: Events): unknown {
    const obj: any = {};
    if (message.events?.length) {
      obj.events = message.events.map((e) => Event.toJSON(e));
    }
    if (message.since !== undefined) {
      obj.since = message.since.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Events>, I>>(base?: I): Events {
    return Events.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Events>, I>>(object: I): Events {
    const message = createBaseEvents();
    message.events = object.events?.map((e) => Event.fromPartial(e)) || [];
    message.since = object.since ?? undefined;
    return message;
  },
};

function createBaseGetFriendshipStatus(): GetFriendshipStatus {
  return { domain: "", secret: "" };
}

export const GetFriendshipStatus: MessageFns<GetFriendshipStatus> = {
  encode(message: GetFriendshipStatus, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.domain !== "") {
      writer.uint32(10).string(message.domain);
    }
    if (message.secret !== "") {
      writer.uint32(18).string(message.secret);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetFriendshipStatus {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetFriendshipStatus();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.domain = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.secret = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetFriendshipStatus {
    return {
      domain: isSet(object.domain) ? globalThis.String(object.domain) : "",
      secret: isSet(object.secret) ? globalThis.String(object.secret) : "",
    };
  },

  toJSON(message: GetFriendshipStatus): unknown {
    const obj: any = {};
    if (message.domain !== "") {
      obj.domain = message.domain;
    }
    if (message.secret !== "") {
      obj.secret = message.secret;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetFriendshipStatus>, I>>(base?: I): GetFriendshipStatus {
    return GetFriendshipStatus.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetFriendshipStatus>, I>>(object: I): GetFriendshipStatus {
    const message = createBaseGetFriendshipStatus();
    message.domain = object.domain ?? "";
    message.secret = object.secret ?? "";
    return message;
  },
};

function createBaseFriendshipStatus(): FriendshipStatus {
  return { status: 0 };
}

export const FriendshipStatus: MessageFns<FriendshipStatus> = {
  encode(message: FriendshipStatus, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.status !== 0) {
      writer.uint32(8).int32(message.status);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FriendshipStatus {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFriendshipStatus();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FriendshipStatus {
    return { status: isSet(object.status) ? friendShipStatusFromJSON(object.status) : 0 };
  },

  toJSON(message: FriendshipStatus): unknown {
    const obj: any = {};
    if (message.status !== 0) {
      obj.status = friendShipStatusToJSON(message.status);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FriendshipStatus>, I>>(base?: I): FriendshipStatus {
    return FriendshipStatus.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FriendshipStatus>, I>>(object: I): FriendshipStatus {
    const message = createBaseFriendshipStatus();
    message.status = object.status ?? 0;
    return message;
  },
};

function createBaseAuthAsFriend(): AuthAsFriend {
  return { domain: "", secret: "" };
}

export const AuthAsFriend: MessageFns<AuthAsFriend> = {
  encode(message: AuthAsFriend, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.domain !== "") {
      writer.uint32(10).string(message.domain);
    }
    if (message.secret !== "") {
      writer.uint32(18).string(message.secret);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AuthAsFriend {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAuthAsFriend();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.domain = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.secret = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AuthAsFriend {
    return {
      domain: isSet(object.domain) ? globalThis.String(object.domain) : "",
      secret: isSet(object.secret) ? globalThis.String(object.secret) : "",
    };
  },

  toJSON(message: AuthAsFriend): unknown {
    const obj: any = {};
    if (message.domain !== "") {
      obj.domain = message.domain;
    }
    if (message.secret !== "") {
      obj.secret = message.secret;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AuthAsFriend>, I>>(base?: I): AuthAsFriend {
    return AuthAsFriend.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AuthAsFriend>, I>>(object: I): AuthAsFriend {
    const message = createBaseAuthAsFriend();
    message.domain = object.domain ?? "";
    message.secret = object.secret ?? "";
    return message;
  },
};

function createBaseReqEnvelope(): ReqEnvelope {
  return { id: 0, payload: undefined };
}

export const ReqEnvelope: MessageFns<ReqEnvelope> = {
  encode(message: ReqEnvelope, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0) {
      writer.uint32(8).int32(message.id);
    }
    switch (message.payload?.$case) {
      case "reqListFiles":
        ListFiles.encode(message.payload.reqListFiles, writer.uint32(82).fork()).join();
        break;
      case "reqGetStatus":
        GetStatus.encode(message.payload.reqGetStatus, writer.uint32(90).fork()).join();
        break;
      case "reqAuth":
        Auth.encode(message.payload.reqAuth, writer.uint32(98).fork()).join();
        break;
      case "reqUploadFile":
        UploadFile.encode(message.payload.reqUploadFile, writer.uint32(106).fork()).join();
        break;
      case "reqGetFile":
        GetFile.encode(message.payload.reqGetFile, writer.uint32(114).fork()).join();
        break;
      case "reqDelFile":
        DelFile.encode(message.payload.reqDelFile, writer.uint32(122).fork()).join();
        break;
      case "reqSearchPhotos":
        SearchPhotos.encode(message.payload.reqSearchPhotos, writer.uint32(130).fork()).join();
        break;
      case "reqGetTags":
        GetTags.encode(message.payload.reqGetTags, writer.uint32(138).fork()).join();
        break;
      case "reqChangeKey":
        ChangeKey.encode(message.payload.reqChangeKey, writer.uint32(146).fork()).join();
        break;
      case "reqGetSettings":
        GetSettings.encode(message.payload.reqGetSettings, writer.uint32(226).fork()).join();
        break;
      case "reqSetSettings":
        SetSettings.encode(message.payload.reqSetSettings, writer.uint32(234).fork()).join();
        break;
      case "reqNewSocialPublication":
        NewSocialPublication.encode(message.payload.reqNewSocialPublication, writer.uint32(154).fork()).join();
        break;
      case "reqGetSocialPublications":
        GetSocialPublications.encode(message.payload.reqGetSocialPublications, writer.uint32(162).fork()).join();
        break;
      case "reqGetSocialPublicationFiles":
        GetSocialPublicationFiles.encode(message.payload.reqGetSocialPublicationFiles, writer.uint32(338).fork())
          .join();
        break;
      case "reqFriendshipRequest":
        FriendshipRequest.encode(message.payload.reqFriendshipRequest, writer.uint32(186).fork()).join();
        break;
      case "reqFriendshipInterRequest":
        FriendshipInterRequest.encode(message.payload.reqFriendshipInterRequest, writer.uint32(298).fork()).join();
        break;
      case "reqDidSendFriendshipReq":
        DidSendFriendshipReq.encode(message.payload.reqDidSendFriendshipReq, writer.uint32(306).fork()).join();
        break;
      case "reqFriendshipsList":
        FriendshipsList.encode(message.payload.reqFriendshipsList, writer.uint32(282).fork()).join();
        break;
      case "reqChangeFriendStatus":
        ChangeFriendStatus.encode(message.payload.reqChangeFriendStatus, writer.uint32(290).fork()).join();
        break;
      case "reqGetFriendshipStatus":
        GetFriendshipStatus.encode(message.payload.reqGetFriendshipStatus, writer.uint32(314).fork()).join();
        break;
      case "reqAuthAsFriend":
        AuthAsFriend.encode(message.payload.reqAuthAsFriend, writer.uint32(322).fork()).join();
        break;
      case "reqNewSocialComment":
        NewSocialComment.encode(message.payload.reqNewSocialComment, writer.uint32(170).fork()).join();
        break;
      case "reqGetEvents":
        GetEvents.encode(message.payload.reqGetEvents, writer.uint32(330).fork()).join();
        break;
      case "reqDelSocialComment":
        DelSocialComment.encode(message.payload.reqDelSocialComment, writer.uint32(178).fork()).join();
        break;
      case "reqLikePublication":
        LikePublication.encode(message.payload.reqLikePublication, writer.uint32(202).fork()).join();
        break;
      case "reqLikeComment":
        LikeComment.encode(message.payload.reqLikeComment, writer.uint32(210).fork()).join();
        break;
      case "reqBridgeRegister":
        BridgeRegister.encode(message.payload.reqBridgeRegister, writer.uint32(242).fork()).join();
        break;
      case "reqGetProfile":
        GetProfile.encode(message.payload.reqGetProfile, writer.uint32(250).fork()).join();
        break;
      case "reqSetProfile":
        Profile.encode(message.payload.reqSetProfile, writer.uint32(258).fork()).join();
        break;
      case "reqShareFilesLink":
        ShareFilesLink.encode(message.payload.reqShareFilesLink, writer.uint32(266).fork()).join();
        break;
      case "reqDownloadSharedLink":
        DownloadSharedLink.encode(message.payload.reqDownloadSharedLink, writer.uint32(274).fork()).join();
        break;
      case "reqGetPubKey":
        GetPubKey.encode(message.payload.reqGetPubKey, writer.uint32(346).fork()).join();
        break;
      case "reqGetPublicationLikers":
        GetPublicationLikers.encode(message.payload.reqGetPublicationLikers, writer.uint32(354).fork()).join();
        break;
      case "reqGetCommentLikers":
        GetCommentLikers.encode(message.payload.reqGetCommentLikers, writer.uint32(362).fork()).join();
        break;
      case "reqDelSocialPublication":
        DelSocialPublication.encode(message.payload.reqDelSocialPublication, writer.uint32(370).fork()).join();
        break;
      case "reqGetFileInfo":
        GetFileInfo.encode(message.payload.reqGetFileInfo, writer.uint32(378).fork()).join();
        break;
      case "reqSetBridgeSecret":
        SetBridgeSecret.encode(message.payload.reqSetBridgeSecret, writer.uint32(386).fork()).join();
        break;
      case "reqListStorageDevices":
        ListStorageDevices.encode(message.payload.reqListStorageDevices, writer.uint32(394).fork()).join();
        break;
      case "reqSetupStorage":
        SetupStorage.encode(message.payload.reqSetupStorage, writer.uint32(402).fork()).join();
        break;
      case "reqRegenerateBridgeSecret":
        RegenerateBridgeSecret.encode(message.payload.reqRegenerateBridgeSecret, writer.uint32(410).fork()).join();
        break;
      case "reqRotateBridgeSecret":
        RotateBridgeSecret.encode(message.payload.reqRotateBridgeSecret, writer.uint32(418).fork()).join();
        break;
      case "reqListWifiNetworks":
        ListWifiNetworks.encode(message.payload.reqListWifiNetworks, writer.uint32(426).fork()).join();
        break;
      case "reqSetWifi":
        SetWifi.encode(message.payload.reqSetWifi, writer.uint32(434).fork()).join();
        break;
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReqEnvelope {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReqEnvelope();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = reader.int32();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.payload = { $case: "reqListFiles", reqListFiles: ListFiles.decode(reader, reader.uint32()) };
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.payload = { $case: "reqGetStatus", reqGetStatus: GetStatus.decode(reader, reader.uint32()) };
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.payload = { $case: "reqAuth", reqAuth: Auth.decode(reader, reader.uint32()) };
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.payload = { $case: "reqUploadFile", reqUploadFile: UploadFile.decode(reader, reader.uint32()) };
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.payload = { $case: "reqGetFile", reqGetFile: GetFile.decode(reader, reader.uint32()) };
          continue;
        }
        case 15: {
          if (tag !== 122) {
            break;
          }

          message.payload = { $case: "reqDelFile", reqDelFile: DelFile.decode(reader, reader.uint32()) };
          continue;
        }
        case 16: {
          if (tag !== 130) {
            break;
          }

          message.payload = { $case: "reqSearchPhotos", reqSearchPhotos: SearchPhotos.decode(reader, reader.uint32()) };
          continue;
        }
        case 17: {
          if (tag !== 138) {
            break;
          }

          message.payload = { $case: "reqGetTags", reqGetTags: GetTags.decode(reader, reader.uint32()) };
          continue;
        }
        case 18: {
          if (tag !== 146) {
            break;
          }

          message.payload = { $case: "reqChangeKey", reqChangeKey: ChangeKey.decode(reader, reader.uint32()) };
          continue;
        }
        case 28: {
          if (tag !== 226) {
            break;
          }

          message.payload = { $case: "reqGetSettings", reqGetSettings: GetSettings.decode(reader, reader.uint32()) };
          continue;
        }
        case 29: {
          if (tag !== 234) {
            break;
          }

          message.payload = { $case: "reqSetSettings", reqSetSettings: SetSettings.decode(reader, reader.uint32()) };
          continue;
        }
        case 19: {
          if (tag !== 154) {
            break;
          }

          message.payload = {
            $case: "reqNewSocialPublication",
            reqNewSocialPublication: NewSocialPublication.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 20: {
          if (tag !== 162) {
            break;
          }

          message.payload = {
            $case: "reqGetSocialPublications",
            reqGetSocialPublications: GetSocialPublications.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 42: {
          if (tag !== 338) {
            break;
          }

          message.payload = {
            $case: "reqGetSocialPublicationFiles",
            reqGetSocialPublicationFiles: GetSocialPublicationFiles.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 23: {
          if (tag !== 186) {
            break;
          }

          message.payload = {
            $case: "reqFriendshipRequest",
            reqFriendshipRequest: FriendshipRequest.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 37: {
          if (tag !== 298) {
            break;
          }

          message.payload = {
            $case: "reqFriendshipInterRequest",
            reqFriendshipInterRequest: FriendshipInterRequest.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 38: {
          if (tag !== 306) {
            break;
          }

          message.payload = {
            $case: "reqDidSendFriendshipReq",
            reqDidSendFriendshipReq: DidSendFriendshipReq.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 35: {
          if (tag !== 282) {
            break;
          }

          message.payload = {
            $case: "reqFriendshipsList",
            reqFriendshipsList: FriendshipsList.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 36: {
          if (tag !== 290) {
            break;
          }

          message.payload = {
            $case: "reqChangeFriendStatus",
            reqChangeFriendStatus: ChangeFriendStatus.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 39: {
          if (tag !== 314) {
            break;
          }

          message.payload = {
            $case: "reqGetFriendshipStatus",
            reqGetFriendshipStatus: GetFriendshipStatus.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 40: {
          if (tag !== 322) {
            break;
          }

          message.payload = { $case: "reqAuthAsFriend", reqAuthAsFriend: AuthAsFriend.decode(reader, reader.uint32()) };
          continue;
        }
        case 21: {
          if (tag !== 170) {
            break;
          }

          message.payload = {
            $case: "reqNewSocialComment",
            reqNewSocialComment: NewSocialComment.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 41: {
          if (tag !== 330) {
            break;
          }

          message.payload = { $case: "reqGetEvents", reqGetEvents: GetEvents.decode(reader, reader.uint32()) };
          continue;
        }
        case 22: {
          if (tag !== 178) {
            break;
          }

          message.payload = {
            $case: "reqDelSocialComment",
            reqDelSocialComment: DelSocialComment.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 25: {
          if (tag !== 202) {
            break;
          }

          message.payload = {
            $case: "reqLikePublication",
            reqLikePublication: LikePublication.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 26: {
          if (tag !== 210) {
            break;
          }

          message.payload = { $case: "reqLikeComment", reqLikeComment: LikeComment.decode(reader, reader.uint32()) };
          continue;
        }
        case 30: {
          if (tag !== 242) {
            break;
          }

          message.payload = {
            $case: "reqBridgeRegister",
            reqBridgeRegister: BridgeRegister.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 31: {
          if (tag !== 250) {
            break;
          }

          message.payload = { $case: "reqGetProfile", reqGetProfile: GetProfile.decode(reader, reader.uint32()) };
          continue;
        }
        case 32: {
          if (tag !== 258) {
            break;
          }

          message.payload = { $case: "reqSetProfile", reqSetProfile: Profile.decode(reader, reader.uint32()) };
          continue;
        }
        case 33: {
          if (tag !== 266) {
            break;
          }

          message.payload = {
            $case: "reqShareFilesLink",
            reqShareFilesLink: ShareFilesLink.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 34: {
          if (tag !== 274) {
            break;
          }

          message.payload = {
            $case: "reqDownloadSharedLink",
            reqDownloadSharedLink: DownloadSharedLink.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 43: {
          if (tag !== 346) {
            break;
          }

          message.payload = { $case: "reqGetPubKey", reqGetPubKey: GetPubKey.decode(reader, reader.uint32()) };
          continue;
        }
        case 44: {
          if (tag !== 354) {
            break;
          }

          message.payload = {
            $case: "reqGetPublicationLikers",
            reqGetPublicationLikers: GetPublicationLikers.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 45: {
          if (tag !== 362) {
            break;
          }

          message.payload = {
            $case: "reqGetCommentLikers",
            reqGetCommentLikers: GetCommentLikers.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 46: {
          if (tag !== 370) {
            break;
          }

          message.payload = {
            $case: "reqDelSocialPublication",
            reqDelSocialPublication: DelSocialPublication.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 47: {
          if (tag !== 378) {
            break;
          }

          message.payload = { $case: "reqGetFileInfo", reqGetFileInfo: GetFileInfo.decode(reader, reader.uint32()) };
          continue;
        }
        case 48: {
          if (tag !== 386) {
            break;
          }

          message.payload = {
            $case: "reqSetBridgeSecret",
            reqSetBridgeSecret: SetBridgeSecret.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 49: {
          if (tag !== 394) {
            break;
          }

          message.payload = {
            $case: "reqListStorageDevices",
            reqListStorageDevices: ListStorageDevices.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 50: {
          if (tag !== 402) {
            break;
          }

          message.payload = { $case: "reqSetupStorage", reqSetupStorage: SetupStorage.decode(reader, reader.uint32()) };
          continue;
        }
        case 51: {
          if (tag !== 410) {
            break;
          }

          message.payload = {
            $case: "reqRegenerateBridgeSecret",
            reqRegenerateBridgeSecret: RegenerateBridgeSecret.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 52: {
          if (tag !== 418) {
            break;
          }

          message.payload = {
            $case: "reqRotateBridgeSecret",
            reqRotateBridgeSecret: RotateBridgeSecret.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 53: {
          if (tag !== 426) {
            break;
          }

          message.payload = {
            $case: "reqListWifiNetworks",
            reqListWifiNetworks: ListWifiNetworks.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 54: {
          if (tag !== 434) {
            break;
          }

          message.payload = { $case: "reqSetWifi", reqSetWifi: SetWifi.decode(reader, reader.uint32()) };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReqEnvelope {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      payload: isSet(object.reqListFiles)
        ? { $case: "reqListFiles", reqListFiles: ListFiles.fromJSON(object.reqListFiles) }
        : isSet(object.reqGetStatus)
        ? { $case: "reqGetStatus", reqGetStatus: GetStatus.fromJSON(object.reqGetStatus) }
        : isSet(object.reqAuth)
        ? { $case: "reqAuth", reqAuth: Auth.fromJSON(object.reqAuth) }
        : isSet(object.reqUploadFile)
        ? { $case: "reqUploadFile", reqUploadFile: UploadFile.fromJSON(object.reqUploadFile) }
        : isSet(object.reqGetFile)
        ? { $case: "reqGetFile", reqGetFile: GetFile.fromJSON(object.reqGetFile) }
        : isSet(object.reqDelFile)
        ? { $case: "reqDelFile", reqDelFile: DelFile.fromJSON(object.reqDelFile) }
        : isSet(object.reqSearchPhotos)
        ? { $case: "reqSearchPhotos", reqSearchPhotos: SearchPhotos.fromJSON(object.reqSearchPhotos) }
        : isSet(object.reqGetTags)
        ? { $case: "reqGetTags", reqGetTags: GetTags.fromJSON(object.reqGetTags) }
        : isSet(object.reqChangeKey)
        ? { $case: "reqChangeKey", reqChangeKey: ChangeKey.fromJSON(object.reqChangeKey) }
        : isSet(object.reqGetSettings)
        ? { $case: "reqGetSettings", reqGetSettings: GetSettings.fromJSON(object.reqGetSettings) }
        : isSet(object.reqSetSettings)
        ? { $case: "reqSetSettings", reqSetSettings: SetSettings.fromJSON(object.reqSetSettings) }
        : isSet(object.reqNewSocialPublication)
        ? {
          $case: "reqNewSocialPublication",
          reqNewSocialPublication: NewSocialPublication.fromJSON(object.reqNewSocialPublication),
        }
        : isSet(object.reqGetSocialPublications)
        ? {
          $case: "reqGetSocialPublications",
          reqGetSocialPublications: GetSocialPublications.fromJSON(object.reqGetSocialPublications),
        }
        : isSet(object.reqGetSocialPublicationFiles)
        ? {
          $case: "reqGetSocialPublicationFiles",
          reqGetSocialPublicationFiles: GetSocialPublicationFiles.fromJSON(object.reqGetSocialPublicationFiles),
        }
        : isSet(object.reqFriendshipRequest)
        ? {
          $case: "reqFriendshipRequest",
          reqFriendshipRequest: FriendshipRequest.fromJSON(object.reqFriendshipRequest),
        }
        : isSet(object.reqFriendshipInterRequest)
        ? {
          $case: "reqFriendshipInterRequest",
          reqFriendshipInterRequest: FriendshipInterRequest.fromJSON(object.reqFriendshipInterRequest),
        }
        : isSet(object.reqDidSendFriendshipReq)
        ? {
          $case: "reqDidSendFriendshipReq",
          reqDidSendFriendshipReq: DidSendFriendshipReq.fromJSON(object.reqDidSendFriendshipReq),
        }
        : isSet(object.reqFriendshipsList)
        ? { $case: "reqFriendshipsList", reqFriendshipsList: FriendshipsList.fromJSON(object.reqFriendshipsList) }
        : isSet(object.reqChangeFriendStatus)
        ? {
          $case: "reqChangeFriendStatus",
          reqChangeFriendStatus: ChangeFriendStatus.fromJSON(object.reqChangeFriendStatus),
        }
        : isSet(object.reqGetFriendshipStatus)
        ? {
          $case: "reqGetFriendshipStatus",
          reqGetFriendshipStatus: GetFriendshipStatus.fromJSON(object.reqGetFriendshipStatus),
        }
        : isSet(object.reqAuthAsFriend)
        ? { $case: "reqAuthAsFriend", reqAuthAsFriend: AuthAsFriend.fromJSON(object.reqAuthAsFriend) }
        : isSet(object.reqNewSocialComment)
        ? { $case: "reqNewSocialComment", reqNewSocialComment: NewSocialComment.fromJSON(object.reqNewSocialComment) }
        : isSet(object.reqGetEvents)
        ? { $case: "reqGetEvents", reqGetEvents: GetEvents.fromJSON(object.reqGetEvents) }
        : isSet(object.reqDelSocialComment)
        ? { $case: "reqDelSocialComment", reqDelSocialComment: DelSocialComment.fromJSON(object.reqDelSocialComment) }
        : isSet(object.reqLikePublication)
        ? { $case: "reqLikePublication", reqLikePublication: LikePublication.fromJSON(object.reqLikePublication) }
        : isSet(object.reqLikeComment)
        ? { $case: "reqLikeComment", reqLikeComment: LikeComment.fromJSON(object.reqLikeComment) }
        : isSet(object.reqBridgeRegister)
        ? { $case: "reqBridgeRegister", reqBridgeRegister: BridgeRegister.fromJSON(object.reqBridgeRegister) }
        : isSet(object.reqGetProfile)
        ? { $case: "reqGetProfile", reqGetProfile: GetProfile.fromJSON(object.reqGetProfile) }
        : isSet(object.reqSetProfile)
        ? { $case: "reqSetProfile", reqSetProfile: Profile.fromJSON(object.reqSetProfile) }
        : isSet(object.reqShareFilesLink)
        ? { $case: "reqShareFilesLink", reqShareFilesLink: ShareFilesLink.fromJSON(object.reqShareFilesLink) }
        : isSet(object.reqDownloadSharedLink)
        ? {
          $case: "reqDownloadSharedLink",
          reqDownloadSharedLink: DownloadSharedLink.fromJSON(object.reqDownloadSharedLink),
        }
        : isSet(object.reqGetPubKey)
        ? { $case: "reqGetPubKey", reqGetPubKey: GetPubKey.fromJSON(object.reqGetPubKey) }
        : isSet(object.reqGetPublicationLikers)
        ? {
          $case: "reqGetPublicationLikers",
          reqGetPublicationLikers: GetPublicationLikers.fromJSON(object.reqGetPublicationLikers),
        }
        : isSet(object.reqGetCommentLikers)
        ? { $case: "reqGetCommentLikers", reqGetCommentLikers: GetCommentLikers.fromJSON(object.reqGetCommentLikers) }
        : isSet(object.reqDelSocialPublication)
        ? {
          $case: "reqDelSocialPublication",
          reqDelSocialPublication: DelSocialPublication.fromJSON(object.reqDelSocialPublication),
        }
        : isSet(object.reqGetFileInfo)
        ? { $case: "reqGetFileInfo", reqGetFileInfo: GetFileInfo.fromJSON(object.reqGetFileInfo) }
        : isSet(object.reqSetBridgeSecret)
        ? { $case: "reqSetBridgeSecret", reqSetBridgeSecret: SetBridgeSecret.fromJSON(object.reqSetBridgeSecret) }
        : isSet(object.reqListStorageDevices)
        ? {
          $case: "reqListStorageDevices",
          reqListStorageDevices: ListStorageDevices.fromJSON(object.reqListStorageDevices),
        }
        : isSet(object.reqSetupStorage)
        ? { $case: "reqSetupStorage", reqSetupStorage: SetupStorage.fromJSON(object.reqSetupStorage) }
        : isSet(object.reqRegenerateBridgeSecret)
        ? {
          $case: "reqRegenerateBridgeSecret",
          reqRegenerateBridgeSecret: RegenerateBridgeSecret.fromJSON(object.reqRegenerateBridgeSecret),
        }
        : isSet(object.reqRotateBridgeSecret)
        ? {
          $case: "reqRotateBridgeSecret",
          reqRotateBridgeSecret: RotateBridgeSecret.fromJSON(object.reqRotateBridgeSecret),
        }
        : isSet(object.reqListWifiNetworks)
        ? { $case: "reqListWifiNetworks", reqListWifiNetworks: ListWifiNetworks.fromJSON(object.reqListWifiNetworks) }
        : isSet(object.reqSetWifi)
        ? { $case: "reqSetWifi", reqSetWifi: SetWifi.fromJSON(object.reqSetWifi) }
        : undefined,
    };
  },

  toJSON(message: ReqEnvelope): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.payload?.$case === "reqListFiles") {
      obj.reqListFiles = ListFiles.toJSON(message.payload.reqListFiles);
    } else if (message.payload?.$case === "reqGetStatus") {
      obj.reqGetStatus = GetStatus.toJSON(message.payload.reqGetStatus);
    } else if (message.payload?.$case === "reqAuth") {
      obj.reqAuth = Auth.toJSON(message.payload.reqAuth);
    } else if (message.payload?.$case === "reqUploadFile") {
      obj.reqUploadFile = UploadFile.toJSON(message.payload.reqUploadFile);
    } else if (message.payload?.$case === "reqGetFile") {
      obj.reqGetFile = GetFile.toJSON(message.payload.reqGetFile);
    } else if (message.payload?.$case === "reqDelFile") {
      obj.reqDelFile = DelFile.toJSON(message.payload.reqDelFile);
    } else if (message.payload?.$case === "reqSearchPhotos") {
      obj.reqSearchPhotos = SearchPhotos.toJSON(message.payload.reqSearchPhotos);
    } else if (message.payload?.$case === "reqGetTags") {
      obj.reqGetTags = GetTags.toJSON(message.payload.reqGetTags);
    } else if (message.payload?.$case === "reqChangeKey") {
      obj.reqChangeKey = ChangeKey.toJSON(message.payload.reqChangeKey);
    } else if (message.payload?.$case === "reqGetSettings") {
      obj.reqGetSettings = GetSettings.toJSON(message.payload.reqGetSettings);
    } else if (message.payload?.$case === "reqSetSettings") {
      obj.reqSetSettings = SetSettings.toJSON(message.payload.reqSetSettings);
    } else if (message.payload?.$case === "reqNewSocialPublication") {
      obj.reqNewSocialPublication = NewSocialPublication.toJSON(message.payload.reqNewSocialPublication);
    } else if (message.payload?.$case === "reqGetSocialPublications") {
      obj.reqGetSocialPublications = GetSocialPublications.toJSON(message.payload.reqGetSocialPublications);
    } else if (message.payload?.$case === "reqGetSocialPublicationFiles") {
      obj.reqGetSocialPublicationFiles = GetSocialPublicationFiles.toJSON(message.payload.reqGetSocialPublicationFiles);
    } else if (message.payload?.$case === "reqFriendshipRequest") {
      obj.reqFriendshipRequest = FriendshipRequest.toJSON(message.payload.reqFriendshipRequest);
    } else if (message.payload?.$case === "reqFriendshipInterRequest") {
      obj.reqFriendshipInterRequest = FriendshipInterRequest.toJSON(message.payload.reqFriendshipInterRequest);
    } else if (message.payload?.$case === "reqDidSendFriendshipReq") {
      obj.reqDidSendFriendshipReq = DidSendFriendshipReq.toJSON(message.payload.reqDidSendFriendshipReq);
    } else if (message.payload?.$case === "reqFriendshipsList") {
      obj.reqFriendshipsList = FriendshipsList.toJSON(message.payload.reqFriendshipsList);
    } else if (message.payload?.$case === "reqChangeFriendStatus") {
      obj.reqChangeFriendStatus = ChangeFriendStatus.toJSON(message.payload.reqChangeFriendStatus);
    } else if (message.payload?.$case === "reqGetFriendshipStatus") {
      obj.reqGetFriendshipStatus = GetFriendshipStatus.toJSON(message.payload.reqGetFriendshipStatus);
    } else if (message.payload?.$case === "reqAuthAsFriend") {
      obj.reqAuthAsFriend = AuthAsFriend.toJSON(message.payload.reqAuthAsFriend);
    } else if (message.payload?.$case === "reqNewSocialComment") {
      obj.reqNewSocialComment = NewSocialComment.toJSON(message.payload.reqNewSocialComment);
    } else if (message.payload?.$case === "reqGetEvents") {
      obj.reqGetEvents = GetEvents.toJSON(message.payload.reqGetEvents);
    } else if (message.payload?.$case === "reqDelSocialComment") {
      obj.reqDelSocialComment = DelSocialComment.toJSON(message.payload.reqDelSocialComment);
    } else if (message.payload?.$case === "reqLikePublication") {
      obj.reqLikePublication = LikePublication.toJSON(message.payload.reqLikePublication);
    } else if (message.payload?.$case === "reqLikeComment") {
      obj.reqLikeComment = LikeComment.toJSON(message.payload.reqLikeComment);
    } else if (message.payload?.$case === "reqBridgeRegister") {
      obj.reqBridgeRegister = BridgeRegister.toJSON(message.payload.reqBridgeRegister);
    } else if (message.payload?.$case === "reqGetProfile") {
      obj.reqGetProfile = GetProfile.toJSON(message.payload.reqGetProfile);
    } else if (message.payload?.$case === "reqSetProfile") {
      obj.reqSetProfile = Profile.toJSON(message.payload.reqSetProfile);
    } else if (message.payload?.$case === "reqShareFilesLink") {
      obj.reqShareFilesLink = ShareFilesLink.toJSON(message.payload.reqShareFilesLink);
    } else if (message.payload?.$case === "reqDownloadSharedLink") {
      obj.reqDownloadSharedLink = DownloadSharedLink.toJSON(message.payload.reqDownloadSharedLink);
    } else if (message.payload?.$case === "reqGetPubKey") {
      obj.reqGetPubKey = GetPubKey.toJSON(message.payload.reqGetPubKey);
    } else if (message.payload?.$case === "reqGetPublicationLikers") {
      obj.reqGetPublicationLikers = GetPublicationLikers.toJSON(message.payload.reqGetPublicationLikers);
    } else if (message.payload?.$case === "reqGetCommentLikers") {
      obj.reqGetCommentLikers = GetCommentLikers.toJSON(message.payload.reqGetCommentLikers);
    } else if (message.payload?.$case === "reqDelSocialPublication") {
      obj.reqDelSocialPublication = DelSocialPublication.toJSON(message.payload.reqDelSocialPublication);
    } else if (message.payload?.$case === "reqGetFileInfo") {
      obj.reqGetFileInfo = GetFileInfo.toJSON(message.payload.reqGetFileInfo);
    } else if (message.payload?.$case === "reqSetBridgeSecret") {
      obj.reqSetBridgeSecret = SetBridgeSecret.toJSON(message.payload.reqSetBridgeSecret);
    } else if (message.payload?.$case === "reqListStorageDevices") {
      obj.reqListStorageDevices = ListStorageDevices.toJSON(message.payload.reqListStorageDevices);
    } else if (message.payload?.$case === "reqSetupStorage") {
      obj.reqSetupStorage = SetupStorage.toJSON(message.payload.reqSetupStorage);
    } else if (message.payload?.$case === "reqRegenerateBridgeSecret") {
      obj.reqRegenerateBridgeSecret = RegenerateBridgeSecret.toJSON(message.payload.reqRegenerateBridgeSecret);
    } else if (message.payload?.$case === "reqRotateBridgeSecret") {
      obj.reqRotateBridgeSecret = RotateBridgeSecret.toJSON(message.payload.reqRotateBridgeSecret);
    } else if (message.payload?.$case === "reqListWifiNetworks") {
      obj.reqListWifiNetworks = ListWifiNetworks.toJSON(message.payload.reqListWifiNetworks);
    } else if (message.payload?.$case === "reqSetWifi") {
      obj.reqSetWifi = SetWifi.toJSON(message.payload.reqSetWifi);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ReqEnvelope>, I>>(base?: I): ReqEnvelope {
    return ReqEnvelope.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReqEnvelope>, I>>(object: I): ReqEnvelope {
    const message = createBaseReqEnvelope();
    message.id = object.id ?? 0;
    switch (object.payload?.$case) {
      case "reqListFiles": {
        if (object.payload?.reqListFiles !== undefined && object.payload?.reqListFiles !== null) {
          message.payload = { $case: "reqListFiles", reqListFiles: ListFiles.fromPartial(object.payload.reqListFiles) };
        }
        break;
      }
      case "reqGetStatus": {
        if (object.payload?.reqGetStatus !== undefined && object.payload?.reqGetStatus !== null) {
          message.payload = { $case: "reqGetStatus", reqGetStatus: GetStatus.fromPartial(object.payload.reqGetStatus) };
        }
        break;
      }
      case "reqAuth": {
        if (object.payload?.reqAuth !== undefined && object.payload?.reqAuth !== null) {
          message.payload = { $case: "reqAuth", reqAuth: Auth.fromPartial(object.payload.reqAuth) };
        }
        break;
      }
      case "reqUploadFile": {
        if (object.payload?.reqUploadFile !== undefined && object.payload?.reqUploadFile !== null) {
          message.payload = {
            $case: "reqUploadFile",
            reqUploadFile: UploadFile.fromPartial(object.payload.reqUploadFile),
          };
        }
        break;
      }
      case "reqGetFile": {
        if (object.payload?.reqGetFile !== undefined && object.payload?.reqGetFile !== null) {
          message.payload = { $case: "reqGetFile", reqGetFile: GetFile.fromPartial(object.payload.reqGetFile) };
        }
        break;
      }
      case "reqDelFile": {
        if (object.payload?.reqDelFile !== undefined && object.payload?.reqDelFile !== null) {
          message.payload = { $case: "reqDelFile", reqDelFile: DelFile.fromPartial(object.payload.reqDelFile) };
        }
        break;
      }
      case "reqSearchPhotos": {
        if (object.payload?.reqSearchPhotos !== undefined && object.payload?.reqSearchPhotos !== null) {
          message.payload = {
            $case: "reqSearchPhotos",
            reqSearchPhotos: SearchPhotos.fromPartial(object.payload.reqSearchPhotos),
          };
        }
        break;
      }
      case "reqGetTags": {
        if (object.payload?.reqGetTags !== undefined && object.payload?.reqGetTags !== null) {
          message.payload = { $case: "reqGetTags", reqGetTags: GetTags.fromPartial(object.payload.reqGetTags) };
        }
        break;
      }
      case "reqChangeKey": {
        if (object.payload?.reqChangeKey !== undefined && object.payload?.reqChangeKey !== null) {
          message.payload = { $case: "reqChangeKey", reqChangeKey: ChangeKey.fromPartial(object.payload.reqChangeKey) };
        }
        break;
      }
      case "reqGetSettings": {
        if (object.payload?.reqGetSettings !== undefined && object.payload?.reqGetSettings !== null) {
          message.payload = {
            $case: "reqGetSettings",
            reqGetSettings: GetSettings.fromPartial(object.payload.reqGetSettings),
          };
        }
        break;
      }
      case "reqSetSettings": {
        if (object.payload?.reqSetSettings !== undefined && object.payload?.reqSetSettings !== null) {
          message.payload = {
            $case: "reqSetSettings",
            reqSetSettings: SetSettings.fromPartial(object.payload.reqSetSettings),
          };
        }
        break;
      }
      case "reqNewSocialPublication": {
        if (object.payload?.reqNewSocialPublication !== undefined && object.payload?.reqNewSocialPublication !== null) {
          message.payload = {
            $case: "reqNewSocialPublication",
            reqNewSocialPublication: NewSocialPublication.fromPartial(object.payload.reqNewSocialPublication),
          };
        }
        break;
      }
      case "reqGetSocialPublications": {
        if (
          object.payload?.reqGetSocialPublications !== undefined && object.payload?.reqGetSocialPublications !== null
        ) {
          message.payload = {
            $case: "reqGetSocialPublications",
            reqGetSocialPublications: GetSocialPublications.fromPartial(object.payload.reqGetSocialPublications),
          };
        }
        break;
      }
      case "reqGetSocialPublicationFiles": {
        if (
          object.payload?.reqGetSocialPublicationFiles !== undefined &&
          object.payload?.reqGetSocialPublicationFiles !== null
        ) {
          message.payload = {
            $case: "reqGetSocialPublicationFiles",
            reqGetSocialPublicationFiles: GetSocialPublicationFiles.fromPartial(
              object.payload.reqGetSocialPublicationFiles,
            ),
          };
        }
        break;
      }
      case "reqFriendshipRequest": {
        if (object.payload?.reqFriendshipRequest !== undefined && object.payload?.reqFriendshipRequest !== null) {
          message.payload = {
            $case: "reqFriendshipRequest",
            reqFriendshipRequest: FriendshipRequest.fromPartial(object.payload.reqFriendshipRequest),
          };
        }
        break;
      }
      case "reqFriendshipInterRequest": {
        if (
          object.payload?.reqFriendshipInterRequest !== undefined && object.payload?.reqFriendshipInterRequest !== null
        ) {
          message.payload = {
            $case: "reqFriendshipInterRequest",
            reqFriendshipInterRequest: FriendshipInterRequest.fromPartial(object.payload.reqFriendshipInterRequest),
          };
        }
        break;
      }
      case "reqDidSendFriendshipReq": {
        if (object.payload?.reqDidSendFriendshipReq !== undefined && object.payload?.reqDidSendFriendshipReq !== null) {
          message.payload = {
            $case: "reqDidSendFriendshipReq",
            reqDidSendFriendshipReq: DidSendFriendshipReq.fromPartial(object.payload.reqDidSendFriendshipReq),
          };
        }
        break;
      }
      case "reqFriendshipsList": {
        if (object.payload?.reqFriendshipsList !== undefined && object.payload?.reqFriendshipsList !== null) {
          message.payload = {
            $case: "reqFriendshipsList",
            reqFriendshipsList: FriendshipsList.fromPartial(object.payload.reqFriendshipsList),
          };
        }
        break;
      }
      case "reqChangeFriendStatus": {
        if (object.payload?.reqChangeFriendStatus !== undefined && object.payload?.reqChangeFriendStatus !== null) {
          message.payload = {
            $case: "reqChangeFriendStatus",
            reqChangeFriendStatus: ChangeFriendStatus.fromPartial(object.payload.reqChangeFriendStatus),
          };
        }
        break;
      }
      case "reqGetFriendshipStatus": {
        if (object.payload?.reqGetFriendshipStatus !== undefined && object.payload?.reqGetFriendshipStatus !== null) {
          message.payload = {
            $case: "reqGetFriendshipStatus",
            reqGetFriendshipStatus: GetFriendshipStatus.fromPartial(object.payload.reqGetFriendshipStatus),
          };
        }
        break;
      }
      case "reqAuthAsFriend": {
        if (object.payload?.reqAuthAsFriend !== undefined && object.payload?.reqAuthAsFriend !== null) {
          message.payload = {
            $case: "reqAuthAsFriend",
            reqAuthAsFriend: AuthAsFriend.fromPartial(object.payload.reqAuthAsFriend),
          };
        }
        break;
      }
      case "reqNewSocialComment": {
        if (object.payload?.reqNewSocialComment !== undefined && object.payload?.reqNewSocialComment !== null) {
          message.payload = {
            $case: "reqNewSocialComment",
            reqNewSocialComment: NewSocialComment.fromPartial(object.payload.reqNewSocialComment),
          };
        }
        break;
      }
      case "reqGetEvents": {
        if (object.payload?.reqGetEvents !== undefined && object.payload?.reqGetEvents !== null) {
          message.payload = { $case: "reqGetEvents", reqGetEvents: GetEvents.fromPartial(object.payload.reqGetEvents) };
        }
        break;
      }
      case "reqDelSocialComment": {
        if (object.payload?.reqDelSocialComment !== undefined && object.payload?.reqDelSocialComment !== null) {
          message.payload = {
            $case: "reqDelSocialComment",
            reqDelSocialComment: DelSocialComment.fromPartial(object.payload.reqDelSocialComment),
          };
        }
        break;
      }
      case "reqLikePublication": {
        if (object.payload?.reqLikePublication !== undefined && object.payload?.reqLikePublication !== null) {
          message.payload = {
            $case: "reqLikePublication",
            reqLikePublication: LikePublication.fromPartial(object.payload.reqLikePublication),
          };
        }
        break;
      }
      case "reqLikeComment": {
        if (object.payload?.reqLikeComment !== undefined && object.payload?.reqLikeComment !== null) {
          message.payload = {
            $case: "reqLikeComment",
            reqLikeComment: LikeComment.fromPartial(object.payload.reqLikeComment),
          };
        }
        break;
      }
      case "reqBridgeRegister": {
        if (object.payload?.reqBridgeRegister !== undefined && object.payload?.reqBridgeRegister !== null) {
          message.payload = {
            $case: "reqBridgeRegister",
            reqBridgeRegister: BridgeRegister.fromPartial(object.payload.reqBridgeRegister),
          };
        }
        break;
      }
      case "reqGetProfile": {
        if (object.payload?.reqGetProfile !== undefined && object.payload?.reqGetProfile !== null) {
          message.payload = {
            $case: "reqGetProfile",
            reqGetProfile: GetProfile.fromPartial(object.payload.reqGetProfile),
          };
        }
        break;
      }
      case "reqSetProfile": {
        if (object.payload?.reqSetProfile !== undefined && object.payload?.reqSetProfile !== null) {
          message.payload = {
            $case: "reqSetProfile",
            reqSetProfile: Profile.fromPartial(object.payload.reqSetProfile),
          };
        }
        break;
      }
      case "reqShareFilesLink": {
        if (object.payload?.reqShareFilesLink !== undefined && object.payload?.reqShareFilesLink !== null) {
          message.payload = {
            $case: "reqShareFilesLink",
            reqShareFilesLink: ShareFilesLink.fromPartial(object.payload.reqShareFilesLink),
          };
        }
        break;
      }
      case "reqDownloadSharedLink": {
        if (object.payload?.reqDownloadSharedLink !== undefined && object.payload?.reqDownloadSharedLink !== null) {
          message.payload = {
            $case: "reqDownloadSharedLink",
            reqDownloadSharedLink: DownloadSharedLink.fromPartial(object.payload.reqDownloadSharedLink),
          };
        }
        break;
      }
      case "reqGetPubKey": {
        if (object.payload?.reqGetPubKey !== undefined && object.payload?.reqGetPubKey !== null) {
          message.payload = { $case: "reqGetPubKey", reqGetPubKey: GetPubKey.fromPartial(object.payload.reqGetPubKey) };
        }
        break;
      }
      case "reqGetPublicationLikers": {
        if (object.payload?.reqGetPublicationLikers !== undefined && object.payload?.reqGetPublicationLikers !== null) {
          message.payload = {
            $case: "reqGetPublicationLikers",
            reqGetPublicationLikers: GetPublicationLikers.fromPartial(object.payload.reqGetPublicationLikers),
          };
        }
        break;
      }
      case "reqGetCommentLikers": {
        if (object.payload?.reqGetCommentLikers !== undefined && object.payload?.reqGetCommentLikers !== null) {
          message.payload = {
            $case: "reqGetCommentLikers",
            reqGetCommentLikers: GetCommentLikers.fromPartial(object.payload.reqGetCommentLikers),
          };
        }
        break;
      }
      case "reqDelSocialPublication": {
        if (object.payload?.reqDelSocialPublication !== undefined && object.payload?.reqDelSocialPublication !== null) {
          message.payload = {
            $case: "reqDelSocialPublication",
            reqDelSocialPublication: DelSocialPublication.fromPartial(object.payload.reqDelSocialPublication),
          };
        }
        break;
      }
      case "reqGetFileInfo": {
        if (object.payload?.reqGetFileInfo !== undefined && object.payload?.reqGetFileInfo !== null) {
          message.payload = {
            $case: "reqGetFileInfo",
            reqGetFileInfo: GetFileInfo.fromPartial(object.payload.reqGetFileInfo),
          };
        }
        break;
      }
      case "reqSetBridgeSecret": {
        if (object.payload?.reqSetBridgeSecret !== undefined && object.payload?.reqSetBridgeSecret !== null) {
          message.payload = {
            $case: "reqSetBridgeSecret",
            reqSetBridgeSecret: SetBridgeSecret.fromPartial(object.payload.reqSetBridgeSecret),
          };
        }
        break;
      }
      case "reqListStorageDevices": {
        if (object.payload?.reqListStorageDevices !== undefined && object.payload?.reqListStorageDevices !== null) {
          message.payload = {
            $case: "reqListStorageDevices",
            reqListStorageDevices: ListStorageDevices.fromPartial(object.payload.reqListStorageDevices),
          };
        }
        break;
      }
      case "reqSetupStorage": {
        if (object.payload?.reqSetupStorage !== undefined && object.payload?.reqSetupStorage !== null) {
          message.payload = {
            $case: "reqSetupStorage",
            reqSetupStorage: SetupStorage.fromPartial(object.payload.reqSetupStorage),
          };
        }
        break;
      }
      case "reqRegenerateBridgeSecret": {
        if (
          object.payload?.reqRegenerateBridgeSecret !== undefined && object.payload?.reqRegenerateBridgeSecret !== null
        ) {
          message.payload = {
            $case: "reqRegenerateBridgeSecret",
            reqRegenerateBridgeSecret: RegenerateBridgeSecret.fromPartial(object.payload.reqRegenerateBridgeSecret),
          };
        }
        break;
      }
      case "reqRotateBridgeSecret": {
        if (object.payload?.reqRotateBridgeSecret !== undefined && object.payload?.reqRotateBridgeSecret !== null) {
          message.payload = {
            $case: "reqRotateBridgeSecret",
            reqRotateBridgeSecret: RotateBridgeSecret.fromPartial(object.payload.reqRotateBridgeSecret),
          };
        }
        break;
      }
      case "reqListWifiNetworks": {
        if (object.payload?.reqListWifiNetworks !== undefined && object.payload?.reqListWifiNetworks !== null) {
          message.payload = {
            $case: "reqListWifiNetworks",
            reqListWifiNetworks: ListWifiNetworks.fromPartial(object.payload.reqListWifiNetworks),
          };
        }
        break;
      }
      case "reqSetWifi": {
        if (object.payload?.reqSetWifi !== undefined && object.payload?.reqSetWifi !== null) {
          message.payload = { $case: "reqSetWifi", reqSetWifi: SetWifi.fromPartial(object.payload.reqSetWifi) };
        }
        break;
      }
    }
    return message;
  },
};

function createBaseRespEnvelope(): RespEnvelope {
  return { id: 0, error: false, errorMessage: "", payload: undefined };
}

export const RespEnvelope: MessageFns<RespEnvelope> = {
  encode(message: RespEnvelope, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0) {
      writer.uint32(8).int32(message.id);
    }
    if (message.error !== false) {
      writer.uint32(16).bool(message.error);
    }
    if (message.errorMessage !== "") {
      writer.uint32(26).string(message.errorMessage);
    }
    switch (message.payload?.$case) {
      case "respStatus":
        Status.encode(message.payload.respStatus, writer.uint32(82).fork()).join();
        break;
      case "respAck":
        Ack.encode(message.payload.respAck, writer.uint32(90).fork()).join();
        break;
      case "respFile":
        File.encode(message.payload.respFile, writer.uint32(98).fork()).join();
        break;
      case "respListOfFiles":
        ListOfFiles.encode(message.payload.respListOfFiles, writer.uint32(106).fork()).join();
        break;
      case "respTagsList":
        TagsList.encode(message.payload.respTagsList, writer.uint32(114).fork()).join();
        break;
      case "respSettings":
        Settings.encode(message.payload.respSettings, writer.uint32(122).fork()).join();
        break;
      case "respBridgeAckOnboard":
        BridgeAckOnboard.encode(message.payload.respBridgeAckOnboard, writer.uint32(130).fork()).join();
        break;
      case "respProfile":
        Profile.encode(message.payload.respProfile, writer.uint32(138).fork()).join();
        break;
      case "respShareLink":
        ShareLink.encode(message.payload.respShareLink, writer.uint32(146).fork()).join();
        break;
      case "respFriendships":
        Friendships.encode(message.payload.respFriendships, writer.uint32(154).fork()).join();
        break;
      case "respSharedFiles":
        SharedFiles.encode(message.payload.respSharedFiles, writer.uint32(162).fork()).join();
        break;
      case "respNewSocial":
        NewSocial.encode(message.payload.respNewSocial, writer.uint32(170).fork()).join();
        break;
      case "respSocialPublications":
        SocialPublications.encode(message.payload.respSocialPublications, writer.uint32(178).fork()).join();
        break;
      case "respSocialPublicationFiles":
        SocialPublicationFiles.encode(message.payload.respSocialPublicationFiles, writer.uint32(202).fork()).join();
        break;
      case "respFriendshipStatus":
        FriendshipStatus.encode(message.payload.respFriendshipStatus, writer.uint32(186).fork()).join();
        break;
      case "respEvents":
        Events.encode(message.payload.respEvents, writer.uint32(194).fork()).join();
        break;
      case "respPubKey":
        PubKey.encode(message.payload.respPubKey, writer.uint32(210).fork()).join();
        break;
      case "respLikers":
        Likers.encode(message.payload.respLikers, writer.uint32(218).fork()).join();
        break;
      case "respFileInfo":
        FileExifInfo.encode(message.payload.respFileInfo, writer.uint32(226).fork()).join();
        break;
      case "respStorageDevices":
        StorageDevices.encode(message.payload.respStorageDevices, writer.uint32(234).fork()).join();
        break;
      case "respRotateBridgeSecretAck":
        RotateBridgeSecretAck.encode(message.payload.respRotateBridgeSecretAck, writer.uint32(242).fork()).join();
        break;
      case "respWifiNetworks":
        WifiNetworks.encode(message.payload.respWifiNetworks, writer.uint32(250).fork()).join();
        break;
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RespEnvelope {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRespEnvelope();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.error = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.errorMessage = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.payload = { $case: "respStatus", respStatus: Status.decode(reader, reader.uint32()) };
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.payload = { $case: "respAck", respAck: Ack.decode(reader, reader.uint32()) };
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.payload = { $case: "respFile", respFile: File.decode(reader, reader.uint32()) };
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.payload = { $case: "respListOfFiles", respListOfFiles: ListOfFiles.decode(reader, reader.uint32()) };
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.payload = { $case: "respTagsList", respTagsList: TagsList.decode(reader, reader.uint32()) };
          continue;
        }
        case 15: {
          if (tag !== 122) {
            break;
          }

          message.payload = { $case: "respSettings", respSettings: Settings.decode(reader, reader.uint32()) };
          continue;
        }
        case 16: {
          if (tag !== 130) {
            break;
          }

          message.payload = {
            $case: "respBridgeAckOnboard",
            respBridgeAckOnboard: BridgeAckOnboard.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 17: {
          if (tag !== 138) {
            break;
          }

          message.payload = { $case: "respProfile", respProfile: Profile.decode(reader, reader.uint32()) };
          continue;
        }
        case 18: {
          if (tag !== 146) {
            break;
          }

          message.payload = { $case: "respShareLink", respShareLink: ShareLink.decode(reader, reader.uint32()) };
          continue;
        }
        case 19: {
          if (tag !== 154) {
            break;
          }

          message.payload = { $case: "respFriendships", respFriendships: Friendships.decode(reader, reader.uint32()) };
          continue;
        }
        case 20: {
          if (tag !== 162) {
            break;
          }

          message.payload = { $case: "respSharedFiles", respSharedFiles: SharedFiles.decode(reader, reader.uint32()) };
          continue;
        }
        case 21: {
          if (tag !== 170) {
            break;
          }

          message.payload = { $case: "respNewSocial", respNewSocial: NewSocial.decode(reader, reader.uint32()) };
          continue;
        }
        case 22: {
          if (tag !== 178) {
            break;
          }

          message.payload = {
            $case: "respSocialPublications",
            respSocialPublications: SocialPublications.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 25: {
          if (tag !== 202) {
            break;
          }

          message.payload = {
            $case: "respSocialPublicationFiles",
            respSocialPublicationFiles: SocialPublicationFiles.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 23: {
          if (tag !== 186) {
            break;
          }

          message.payload = {
            $case: "respFriendshipStatus",
            respFriendshipStatus: FriendshipStatus.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 24: {
          if (tag !== 194) {
            break;
          }

          message.payload = { $case: "respEvents", respEvents: Events.decode(reader, reader.uint32()) };
          continue;
        }
        case 26: {
          if (tag !== 210) {
            break;
          }

          message.payload = { $case: "respPubKey", respPubKey: PubKey.decode(reader, reader.uint32()) };
          continue;
        }
        case 27: {
          if (tag !== 218) {
            break;
          }

          message.payload = { $case: "respLikers", respLikers: Likers.decode(reader, reader.uint32()) };
          continue;
        }
        case 28: {
          if (tag !== 226) {
            break;
          }

          message.payload = { $case: "respFileInfo", respFileInfo: FileExifInfo.decode(reader, reader.uint32()) };
          continue;
        }
        case 29: {
          if (tag !== 234) {
            break;
          }

          message.payload = {
            $case: "respStorageDevices",
            respStorageDevices: StorageDevices.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 30: {
          if (tag !== 242) {
            break;
          }

          message.payload = {
            $case: "respRotateBridgeSecretAck",
            respRotateBridgeSecretAck: RotateBridgeSecretAck.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 31: {
          if (tag !== 250) {
            break;
          }

          message.payload = {
            $case: "respWifiNetworks",
            respWifiNetworks: WifiNetworks.decode(reader, reader.uint32()),
          };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RespEnvelope {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      error: isSet(object.error) ? globalThis.Boolean(object.error) : false,
      errorMessage: isSet(object.errorMessage) ? globalThis.String(object.errorMessage) : "",
      payload: isSet(object.respStatus)
        ? { $case: "respStatus", respStatus: Status.fromJSON(object.respStatus) }
        : isSet(object.respAck)
        ? { $case: "respAck", respAck: Ack.fromJSON(object.respAck) }
        : isSet(object.respFile)
        ? { $case: "respFile", respFile: File.fromJSON(object.respFile) }
        : isSet(object.respListOfFiles)
        ? { $case: "respListOfFiles", respListOfFiles: ListOfFiles.fromJSON(object.respListOfFiles) }
        : isSet(object.respTagsList)
        ? { $case: "respTagsList", respTagsList: TagsList.fromJSON(object.respTagsList) }
        : isSet(object.respSettings)
        ? { $case: "respSettings", respSettings: Settings.fromJSON(object.respSettings) }
        : isSet(object.respBridgeAckOnboard)
        ? {
          $case: "respBridgeAckOnboard",
          respBridgeAckOnboard: BridgeAckOnboard.fromJSON(object.respBridgeAckOnboard),
        }
        : isSet(object.respProfile)
        ? { $case: "respProfile", respProfile: Profile.fromJSON(object.respProfile) }
        : isSet(object.respShareLink)
        ? { $case: "respShareLink", respShareLink: ShareLink.fromJSON(object.respShareLink) }
        : isSet(object.respFriendships)
        ? { $case: "respFriendships", respFriendships: Friendships.fromJSON(object.respFriendships) }
        : isSet(object.respSharedFiles)
        ? { $case: "respSharedFiles", respSharedFiles: SharedFiles.fromJSON(object.respSharedFiles) }
        : isSet(object.respNewSocial)
        ? { $case: "respNewSocial", respNewSocial: NewSocial.fromJSON(object.respNewSocial) }
        : isSet(object.respSocialPublications)
        ? {
          $case: "respSocialPublications",
          respSocialPublications: SocialPublications.fromJSON(object.respSocialPublications),
        }
        : isSet(object.respSocialPublicationFiles)
        ? {
          $case: "respSocialPublicationFiles",
          respSocialPublicationFiles: SocialPublicationFiles.fromJSON(object.respSocialPublicationFiles),
        }
        : isSet(object.respFriendshipStatus)
        ? {
          $case: "respFriendshipStatus",
          respFriendshipStatus: FriendshipStatus.fromJSON(object.respFriendshipStatus),
        }
        : isSet(object.respEvents)
        ? { $case: "respEvents", respEvents: Events.fromJSON(object.respEvents) }
        : isSet(object.respPubKey)
        ? { $case: "respPubKey", respPubKey: PubKey.fromJSON(object.respPubKey) }
        : isSet(object.respLikers)
        ? { $case: "respLikers", respLikers: Likers.fromJSON(object.respLikers) }
        : isSet(object.respFileInfo)
        ? { $case: "respFileInfo", respFileInfo: FileExifInfo.fromJSON(object.respFileInfo) }
        : isSet(object.respStorageDevices)
        ? { $case: "respStorageDevices", respStorageDevices: StorageDevices.fromJSON(object.respStorageDevices) }
        : isSet(object.respRotateBridgeSecretAck)
        ? {
          $case: "respRotateBridgeSecretAck",
          respRotateBridgeSecretAck: RotateBridgeSecretAck.fromJSON(object.respRotateBridgeSecretAck),
        }
        : isSet(object.respWifiNetworks)
        ? { $case: "respWifiNetworks", respWifiNetworks: WifiNetworks.fromJSON(object.respWifiNetworks) }
        : undefined,
    };
  },

  toJSON(message: RespEnvelope): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.error !== false) {
      obj.error = message.error;
    }
    if (message.errorMessage !== "") {
      obj.errorMessage = message.errorMessage;
    }
    if (message.payload?.$case === "respStatus") {
      obj.respStatus = Status.toJSON(message.payload.respStatus);
    } else if (message.payload?.$case === "respAck") {
      obj.respAck = Ack.toJSON(message.payload.respAck);
    } else if (message.payload?.$case === "respFile") {
      obj.respFile = File.toJSON(message.payload.respFile);
    } else if (message.payload?.$case === "respListOfFiles") {
      obj.respListOfFiles = ListOfFiles.toJSON(message.payload.respListOfFiles);
    } else if (message.payload?.$case === "respTagsList") {
      obj.respTagsList = TagsList.toJSON(message.payload.respTagsList);
    } else if (message.payload?.$case === "respSettings") {
      obj.respSettings = Settings.toJSON(message.payload.respSettings);
    } else if (message.payload?.$case === "respBridgeAckOnboard") {
      obj.respBridgeAckOnboard = BridgeAckOnboard.toJSON(message.payload.respBridgeAckOnboard);
    } else if (message.payload?.$case === "respProfile") {
      obj.respProfile = Profile.toJSON(message.payload.respProfile);
    } else if (message.payload?.$case === "respShareLink") {
      obj.respShareLink = ShareLink.toJSON(message.payload.respShareLink);
    } else if (message.payload?.$case === "respFriendships") {
      obj.respFriendships = Friendships.toJSON(message.payload.respFriendships);
    } else if (message.payload?.$case === "respSharedFiles") {
      obj.respSharedFiles = SharedFiles.toJSON(message.payload.respSharedFiles);
    } else if (message.payload?.$case === "respNewSocial") {
      obj.respNewSocial = NewSocial.toJSON(message.payload.respNewSocial);
    } else if (message.payload?.$case === "respSocialPublications") {
      obj.respSocialPublications = SocialPublications.toJSON(message.payload.respSocialPublications);
    } else if (message.payload?.$case === "respSocialPublicationFiles") {
      obj.respSocialPublicationFiles = SocialPublicationFiles.toJSON(message.payload.respSocialPublicationFiles);
    } else if (message.payload?.$case === "respFriendshipStatus") {
      obj.respFriendshipStatus = FriendshipStatus.toJSON(message.payload.respFriendshipStatus);
    } else if (message.payload?.$case === "respEvents") {
      obj.respEvents = Events.toJSON(message.payload.respEvents);
    } else if (message.payload?.$case === "respPubKey") {
      obj.respPubKey = PubKey.toJSON(message.payload.respPubKey);
    } else if (message.payload?.$case === "respLikers") {
      obj.respLikers = Likers.toJSON(message.payload.respLikers);
    } else if (message.payload?.$case === "respFileInfo") {
      obj.respFileInfo = FileExifInfo.toJSON(message.payload.respFileInfo);
    } else if (message.payload?.$case === "respStorageDevices") {
      obj.respStorageDevices = StorageDevices.toJSON(message.payload.respStorageDevices);
    } else if (message.payload?.$case === "respRotateBridgeSecretAck") {
      obj.respRotateBridgeSecretAck = RotateBridgeSecretAck.toJSON(message.payload.respRotateBridgeSecretAck);
    } else if (message.payload?.$case === "respWifiNetworks") {
      obj.respWifiNetworks = WifiNetworks.toJSON(message.payload.respWifiNetworks);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RespEnvelope>, I>>(base?: I): RespEnvelope {
    return RespEnvelope.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RespEnvelope>, I>>(object: I): RespEnvelope {
    const message = createBaseRespEnvelope();
    message.id = object.id ?? 0;
    message.error = object.error ?? false;
    message.errorMessage = object.errorMessage ?? "";
    switch (object.payload?.$case) {
      case "respStatus": {
        if (object.payload?.respStatus !== undefined && object.payload?.respStatus !== null) {
          message.payload = { $case: "respStatus", respStatus: Status.fromPartial(object.payload.respStatus) };
        }
        break;
      }
      case "respAck": {
        if (object.payload?.respAck !== undefined && object.payload?.respAck !== null) {
          message.payload = { $case: "respAck", respAck: Ack.fromPartial(object.payload.respAck) };
        }
        break;
      }
      case "respFile": {
        if (object.payload?.respFile !== undefined && object.payload?.respFile !== null) {
          message.payload = { $case: "respFile", respFile: File.fromPartial(object.payload.respFile) };
        }
        break;
      }
      case "respListOfFiles": {
        if (object.payload?.respListOfFiles !== undefined && object.payload?.respListOfFiles !== null) {
          message.payload = {
            $case: "respListOfFiles",
            respListOfFiles: ListOfFiles.fromPartial(object.payload.respListOfFiles),
          };
        }
        break;
      }
      case "respTagsList": {
        if (object.payload?.respTagsList !== undefined && object.payload?.respTagsList !== null) {
          message.payload = { $case: "respTagsList", respTagsList: TagsList.fromPartial(object.payload.respTagsList) };
        }
        break;
      }
      case "respSettings": {
        if (object.payload?.respSettings !== undefined && object.payload?.respSettings !== null) {
          message.payload = { $case: "respSettings", respSettings: Settings.fromPartial(object.payload.respSettings) };
        }
        break;
      }
      case "respBridgeAckOnboard": {
        if (object.payload?.respBridgeAckOnboard !== undefined && object.payload?.respBridgeAckOnboard !== null) {
          message.payload = {
            $case: "respBridgeAckOnboard",
            respBridgeAckOnboard: BridgeAckOnboard.fromPartial(object.payload.respBridgeAckOnboard),
          };
        }
        break;
      }
      case "respProfile": {
        if (object.payload?.respProfile !== undefined && object.payload?.respProfile !== null) {
          message.payload = { $case: "respProfile", respProfile: Profile.fromPartial(object.payload.respProfile) };
        }
        break;
      }
      case "respShareLink": {
        if (object.payload?.respShareLink !== undefined && object.payload?.respShareLink !== null) {
          message.payload = {
            $case: "respShareLink",
            respShareLink: ShareLink.fromPartial(object.payload.respShareLink),
          };
        }
        break;
      }
      case "respFriendships": {
        if (object.payload?.respFriendships !== undefined && object.payload?.respFriendships !== null) {
          message.payload = {
            $case: "respFriendships",
            respFriendships: Friendships.fromPartial(object.payload.respFriendships),
          };
        }
        break;
      }
      case "respSharedFiles": {
        if (object.payload?.respSharedFiles !== undefined && object.payload?.respSharedFiles !== null) {
          message.payload = {
            $case: "respSharedFiles",
            respSharedFiles: SharedFiles.fromPartial(object.payload.respSharedFiles),
          };
        }
        break;
      }
      case "respNewSocial": {
        if (object.payload?.respNewSocial !== undefined && object.payload?.respNewSocial !== null) {
          message.payload = {
            $case: "respNewSocial",
            respNewSocial: NewSocial.fromPartial(object.payload.respNewSocial),
          };
        }
        break;
      }
      case "respSocialPublications": {
        if (object.payload?.respSocialPublications !== undefined && object.payload?.respSocialPublications !== null) {
          message.payload = {
            $case: "respSocialPublications",
            respSocialPublications: SocialPublications.fromPartial(object.payload.respSocialPublications),
          };
        }
        break;
      }
      case "respSocialPublicationFiles": {
        if (
          object.payload?.respSocialPublicationFiles !== undefined &&
          object.payload?.respSocialPublicationFiles !== null
        ) {
          message.payload = {
            $case: "respSocialPublicationFiles",
            respSocialPublicationFiles: SocialPublicationFiles.fromPartial(object.payload.respSocialPublicationFiles),
          };
        }
        break;
      }
      case "respFriendshipStatus": {
        if (object.payload?.respFriendshipStatus !== undefined && object.payload?.respFriendshipStatus !== null) {
          message.payload = {
            $case: "respFriendshipStatus",
            respFriendshipStatus: FriendshipStatus.fromPartial(object.payload.respFriendshipStatus),
          };
        }
        break;
      }
      case "respEvents": {
        if (object.payload?.respEvents !== undefined && object.payload?.respEvents !== null) {
          message.payload = { $case: "respEvents", respEvents: Events.fromPartial(object.payload.respEvents) };
        }
        break;
      }
      case "respPubKey": {
        if (object.payload?.respPubKey !== undefined && object.payload?.respPubKey !== null) {
          message.payload = { $case: "respPubKey", respPubKey: PubKey.fromPartial(object.payload.respPubKey) };
        }
        break;
      }
      case "respLikers": {
        if (object.payload?.respLikers !== undefined && object.payload?.respLikers !== null) {
          message.payload = { $case: "respLikers", respLikers: Likers.fromPartial(object.payload.respLikers) };
        }
        break;
      }
      case "respFileInfo": {
        if (object.payload?.respFileInfo !== undefined && object.payload?.respFileInfo !== null) {
          message.payload = {
            $case: "respFileInfo",
            respFileInfo: FileExifInfo.fromPartial(object.payload.respFileInfo),
          };
        }
        break;
      }
      case "respStorageDevices": {
        if (object.payload?.respStorageDevices !== undefined && object.payload?.respStorageDevices !== null) {
          message.payload = {
            $case: "respStorageDevices",
            respStorageDevices: StorageDevices.fromPartial(object.payload.respStorageDevices),
          };
        }
        break;
      }
      case "respRotateBridgeSecretAck": {
        if (
          object.payload?.respRotateBridgeSecretAck !== undefined && object.payload?.respRotateBridgeSecretAck !== null
        ) {
          message.payload = {
            $case: "respRotateBridgeSecretAck",
            respRotateBridgeSecretAck: RotateBridgeSecretAck.fromPartial(object.payload.respRotateBridgeSecretAck),
          };
        }
        break;
      }
      case "respWifiNetworks": {
        if (object.payload?.respWifiNetworks !== undefined && object.payload?.respWifiNetworks !== null) {
          message.payload = {
            $case: "respWifiNetworks",
            respWifiNetworks: WifiNetworks.fromPartial(object.payload.respWifiNetworks),
          };
        }
        break;
      }
    }
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = BigInt(Math.trunc(date.getTime() / 1_000));
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (globalThis.Number(t.seconds.toString()) || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
